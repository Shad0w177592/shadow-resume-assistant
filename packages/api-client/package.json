{
  "name": "@shadow/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "format": "prettier --write src",
    "test": "node --test tests/*.test.mjs",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "prettier": "latest",
    "typescript": "latest"
  }
}

