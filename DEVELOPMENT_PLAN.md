# 影子简历助手 V1.0 开发计划

> 依据：[PRD.md](./PRD.md)  
> 状态：可执行草案  
> 目标平台：Windows 10 / Windows 11  
> 制定日期：2026-08-22

## 1. 计划目标

本计划用于把 PRD 拆解成可以按顺序执行、逐阶段验收的开发工作。V1.0 的最终交付物是一个无需用户安装 Python 或 Node.js、可在 Windows 10/11 本地安装运行的“影子简历助手”安装包。

完成标准不是“页面能打开”，而是 PRD 第 26 章的验收项目全部通过，并具备以下完整闭环：

```text
安装启动
→ 配置 API
→ 手动填写或导入个人资料
→ 粘贴岗位 JD
→ 分析岗位并匹配证据
→ 配置并生成简历
→ 文字/语音修改
→ 保存历史版本
→ 导出 Word/PDF
→ 备份、恢复和覆盖安装验证
```

## 2. 交付策略

### 2.1 开发原则

1. 先验证高风险能力，再建设完整界面；
2. 先跑通一个最小纵向闭环，再扩展所有栏目和模板；
3. AI 流程采用确定性状态机和结构化输出，不做无边界自主 Agent；
4. 所有 AI 结果先落入候选结果，不直接覆盖用户数据；
5. 所有生成内容必须携带证据引用，确定性检查优先于二次模型检查；
6. 本地数据和数据库迁移从第一天开始设计，不能后期补救；
7. Windows 打包必须在开发早期完成技术验证，不能留到最后；
8. 每个阶段都有退出条件，未达标不进入下一阶段。

### 2.2 建议总体工期

按一名开发者全职计算，建议预留 14～18 周；按业余时间开发，建议预留 5～7 个月。以下估算为理想人日，不包含长期等待外部 API、证书或设备的时间。

| 阶段 | 预计人日 | 主要结果 |
|---|---:|---|
| 0. 风险验证与技术定型 | 6～8 | 打包、语音、PDF/DOCX、AI 结构化输出可行 |
| 1. 工程骨架与质量基线 | 5～7 | 可启动的桌面空壳、测试和构建流水线 |
| 2. 本地数据、安全与首次启动 | 8～10 | SQLite、目录、API Key、设置、初始化 |
| 3. 个人资料与手动纵向闭环 | 10～12 | 手填资料、岗位、最简生成闭环 |
| 4. 文件导入与资料检查 | 10～12 | PDF/DOCX/作品集导入和用户确认 |
| 5. 岗位分析与证据匹配 | 10～12 | 匹配报告和可追溯证据 |
| 6. 简历配置、生成与工作台 | 14～18 | 两模板、栏目配置、编辑、事实检查 |
| 7. AI 修改与语音 | 7～9 | 定向修改、差异确认、语音转写 |
| 8. 版本、导出、备份与恢复 | 10～13 | Word/PDF、历史版本、备份恢复 |
| 9. 稳定性、安装包与最终验收 | 12～16 | Windows 安装包和完整验收报告 |

总量约 92～117 人日。若需要压缩周期，应减少第一版范围，而不是跳过数据安全、事实检查或安装测试。

## 3. 技术架构决策

### 3.1 推荐架构

```text
Electron 主进程
├─ Windows 窗口、文件选择、保存对话框
├─ 启动/停止 Python 后端进程
├─ PDF 打印、应用目录和安装生命周期
└─ 安全 IPC

React + TypeScript 渲染进程
├─ 页面和表单
├─ 简历实时预览
├─ 音频录制
├─ AI 任务进度
└─ 与本地 FastAPI 通信

FastAPI + Python 本地后端
├─ SQLite 和文件存储
├─ 文档解析
├─ AI 工作流
├─ 隐私脱敏与事实校验
├─ DOCX 导出
├─ 备份恢复
└─ OpenAI/后续 DeepSeek 供应商适配层
```

### 3.2 选择 Electron 的原因

- React 界面可以直接运行在成熟的 Chromium 环境；
- 麦克风录制、拖拽、打印预览和 PDF 导出能力稳定；
- 可以通过安装包同时分发 Node/Electron 和打包后的 Python 后端；
- 后续增加自动更新和代码签名路径清晰；
- 缺点是安装包较大，但 V1.0 可接受。

### 3.3 本地后端安全边界

- FastAPI 只绑定 `127.0.0.1`，禁止绑定所有网卡；
- 后端使用系统分配的临时端口，避免固定端口冲突；
- Electron 启动后端时生成一次性会话令牌；
- 前端请求必须携带会话令牌；
- CORS 只允许应用自身来源；
- Electron 禁用不必要的 Node 集成，使用受控 preload API；
- 所有文件路径必须规范化并验证位于允许目录；
- 禁止日志记录完整 API Key 和未脱敏简历正文。

### 3.4 建议工程目录

```text
shadow-resume-assistant/
├─ apps/
│  ├─ desktop/                 # Electron main、preload、安装配置
│  └─ web/                     # React + TypeScript
├─ backend/
│  ├─ app/
│  │  ├─ api/                  # FastAPI 路由
│  │  ├─ domain/               # 领域模型和业务规则
│  │  ├─ services/             # 文档、AI、导出、备份服务
│  │  ├─ providers/            # OpenAI 等供应商适配器
│  │  ├─ persistence/          # SQLite、迁移、仓储
│  │  ├─ security/             # 脱敏、凭据、路径检查
│  │  └─ main.py
│  ├─ migrations/
│  └─ tests/
├─ packages/
│  ├─ api-client/              # 从 OpenAPI 生成的 TS 客户端
│  ├─ resume-schema/           # 前后端共享的 JSON Schema
│  └─ ui/                      # 公共 UI 组件
├─ resources/
│  ├─ resume-templates/
│  ├─ fonts/
│  └─ demo-data/
├─ tests/
│  ├─ e2e/
│  ├─ fixtures/
│  ├─ golden-ai/
│  └─ windows/
├─ docs/
├─ PRD.md
└─ DEVELOPMENT_PLAN.md
```

### 3.5 数据目录

运行时数据不得写入安装目录。建议使用：

```text
%LOCALAPPDATA%\ShadowResumeAssistant\
├─ data\app.db
├─ documents\imports\
├─ documents\photos\
├─ exports\
├─ backups\
├─ temp\
└─ logs\
```

API Key 保存到 Windows Credential Manager，不写入 SQLite、备份或日志。

## 4. 工程质量基线

### 4.1 统一命令

项目初始化后必须提供以下统一命令，具体由工作区脚本封装：

- `dev`：同时启动 Electron、React 和 FastAPI；
- `lint`：运行 TypeScript、Python 静态检查；
- `format`：统一格式化；
- `test`：运行前后端单元测试；
- `test:integration`：运行数据库、AI mock、文档与导出集成测试；
- `test:e2e`：运行关键用户流程；
- `build`：生成前端和 Python 后端产物；
- `package:win`：生成 Windows 安装包；
- `verify:package`：在干净环境执行安装冒烟测试。

### 4.2 提交门槛

每个功能进入主分支前必须满足：

- 代码格式和静态检查通过；
- 新增业务规则有单元测试；
- 数据库变更包含迁移和回滚说明；
- API 变更同步 OpenAPI 客户端；
- 不提交真实 API Key、真实简历和个人隐私数据；
- 关键用户流程的自动化测试不回退。

### 4.3 Definition of Done

一个任务只有同时满足以下条件才算完成：

1. 功能代码完成；
2. 空白、加载、成功和失败状态完成；
3. 键盘操作和基本无障碍标签完成；
4. 单元或集成测试完成；
5. 日志不包含敏感信息；
6. PRD 对应验收项可实际复现；
7. 相关文档更新。

## 5. 阶段 0：高风险技术验证

本阶段只做最小实验，不建设正式业务页面。实验代码验证后可以保留为测试工具，但不得直接当成生产实现。

### 0.1 桌面打包验证

- 建立最小 Electron + React 窗口；
- 用 PyInstaller 打包最小 FastAPI 后端；
- Electron 启动后端并通过随机端口请求健康检查；
- 生成未签名 Windows 安装包；
- 在一台未安装 Python/Node 的 Windows 测试机安装和启动；
- 验证卸载不删除独立数据目录。

退出条件：干净测试机可安装、启动、调用后端并正常关闭，后台无残留进程。

### 0.2 麦克风和语音验证

- 在 Electron 渲染进程中请求麦克风权限；
- 录制 10～30 秒中文音频；
- 发送到 OpenAI 转写接口；
- 展示转写文字并删除临时音频；
- 验证拒绝权限、无麦克风和网络失败状态。

退出条件：Windows 10/11 均能完成录音、转写、确认和清理。

### 0.3 文档导入验证

- 使用 Python 读取可选择文字的 PDF；
- 使用 Python 读取 `.docx`；
- 识别扫描 PDF、加密 PDF、损坏文件；
- 输出统一的段落、表格和页码中间结构；
- 使用 5 份 PDF 和 5 份 DOCX 样本记录成功率和失败原因。

退出条件：所有样本均能成功解析或给出明确、可分类的失败原因，程序不崩溃。

### 0.4 简历导出验证

- 定义与 UI 无关的 `ResumeDocument` 中间模型；
- 用同一中间模型生成 HTML 预览、PDF 和 DOCX；
- 生成单栏一页、单栏两页、双栏一页、双栏两页样本；
- 验证中文字体、分页、照片和链接；
- 比较预览、PDF、DOCX 的内容一致性。

退出条件：四类样本均可打开，页数符合选择，无乱码和明显越界。

### 0.5 AI 结构化输出与事实约束验证

- 定义岗位分析、证据匹配、简历生成的 JSON Schema；
- 使用脱敏后的合成资料和 JD 调用当前可用 OpenAI 模型；
- 验证结构化输出、重试和取消；
- 每条生成内容必须返回 `source_entry_ids`；
- 对公司、岗位、时间、数字和专有技能做确定性白名单检查；
- 使用至少 20 组测试样例验证不会凭空增加硬事实。

退出条件：Schema 解析成功率达到 98%（允许一次自动重试），测试集硬事实编造为 0。

### 0.6 阶段交付物

- `docs/architecture-spike.md`；
- 四项风险实验结果；
- 最终桌面壳和导出技术决定；
- 安装包体积和启动时间基线；
- 已知限制清单。

## 6. 阶段 1：工程骨架与设计基线

### 1.1 工程初始化

- 初始化 Electron、React/TypeScript、FastAPI/Python 工作区；
- 配置开发、测试、构建和打包命令；
- 配置代码格式、静态检查和提交前检查；
- 建立 OpenAPI 到 TypeScript 客户端生成流程；
- 建立环境变量示例，但不允许存真实 Key。

### 1.2 应用壳

- 实现窗口标题“影子简历助手”；
- 实现左侧导航：首页、个人资料、目标岗位、设置；
- 实现路由、全局错误边界和通知系统；
- 实现 Electron 主进程和 FastAPI 生命周期管理；
- 实现单实例运行和正常退出。

### 1.3 视觉基础

- 根据后续视觉规范建立颜色、字体、间距和圆角变量；
- 建立按钮、输入框、卡片、对话框、步骤条、空状态和进度组件；
- 建立简历画布尺寸和打印 CSS 基线；
- 所有页面先使用真实中文文案，不使用无意义 Lorem Ipsum。

### 1.4 测试基础

- 后端单元测试框架；
- 前端组件测试框架；
- API 集成测试；
- Electron/React 关键流程 E2E；
- Mock OpenAI 服务，默认测试不得消耗真实 API 费用。

退出条件：开发命令可一键启动，空壳页面可导航，测试、构建和最小安装包均通过。

## 7. 阶段 2：本地数据、安全和首次启动

### 2.1 数据模型 V1

建立 SQLite 表和首个迁移，至少包括：

- `user_profile`；
- `profile_section_entry`；
- `source_document`；
- `job_target`；
- `job_requirement`；
- `evidence_link`；
- `resume_config`；
- `resume_draft`；
- `resume_version`；
- `edit_proposal`；
- `app_setting`；
- `task_run`；
- `backup_record`。

关键要求：

- 使用 UUID，避免导入恢复时自增 ID 冲突；
- 所有主要表记录创建和更新时间；
- 草稿和版本分离；
- 软删除只用于需要恢复的业务数据，敏感数据清除必须物理删除；
- JSON 扩展字段必须有 Schema 版本。

### 2.2 文件存储服务

- 初始化 AppData 目录；
- 导入文件使用内部 UUID 文件名，原始名保存在数据库；
- 文件复制使用临时文件加原子重命名；
- 校验扩展名、MIME、大小和哈希；
- 提供临时文件定期清理；
- 实现路径穿越防护。

### 2.3 API Key 安全存储

- 通过 Windows Credential Manager 保存、读取和删除 Key；
- UI 只显示掩码；
- 测试新 Key 成功后才替换旧 Key；
- 日志、异常和遥测对象统一脱敏；
- 增加“API Key 不进入备份”自动化测试。

### 2.4 首次启动

- 隐私说明与确认；
- API 配置和跳过；
- 导入简历或从空白开始；
- 初始化完成页；
- 中途退出后可以从合理步骤继续。

### 2.5 设置页基础

- AI 服务；
- 语音设备占位和权限状态；
- 数据目录展示与打开；
- 软件信息；
- 危险操作区域暂用功能开关隐藏，等阶段 8 接入。

退出条件：首次启动闭环完成，API Key 不落盘到 SQLite/日志，重启后配置和数据可用。

## 8. 阶段 3：个人资料与最小纵向闭环

本阶段先手动填写，不依赖文件导入，目标是尽快跑通“资料→岗位→生成一份简历草稿”。

### 3.1 个人资料页面

- 个人信息和照片；
- 教育、工作、实习、项目、校园、技能、证书；
- 灵活的其他经历；
- 所有栏目和字段选填；
- 新增、编辑、复制、删除；
- 自动保存状态；
- 空栏目不计入完整度、不阻断流程。

### 3.2 首页

- API 状态；
- 当前草稿；
- 资料状态和可忽略建议；
- 新建岗位、导入简历入口；
- 最近岗位和最近版本占位数据接入；
- 本地数据目录。

### 3.3 目标岗位基础

- 岗位列表；
- 新建、编辑、复制、删除；
- 公司、岗位、JD、备注；
- 状态流转；
- 每个岗位数据隔离。

### 3.4 最小生成闭环

- 暂时只支持简洁单栏和手动资料；
- JD 分析先使用结构化 AI 输出；
- 从资料条目选择相关内容；
- 生成 `ResumeDocument`；
- 展示只读预览；
- 自动保存草稿。

退出条件：用户可以纯手工填写资料、粘贴 JD、生成一份有证据引用的单栏简历草稿，重启后草稿仍在。

## 9. 阶段 4：文件导入与 AI 资料检查

### 4.1 本地文档解析

- PDF 文本和页码提取；
- DOCX 段落、列表和表格提取；
- Markdown、TXT 读取；
- 扫描、加密、损坏和不支持文件检测；
- 统一 `ParsedDocument` 中间结构。

### 4.2 AI 归类

- 将解析结果脱敏；
- AI 生成候选资料条目；
- 每条候选内容保留来源文件、页码或段落位置；
- 标记明确、不确定、无法归类和疑似重复；
- 不确定内容不得直接写入正式资料。

### 4.3 导入确认页面

- 原文与 AI 归类左右对照；
- 确认、修改归类、忽略；
- 合并、保留两条、替换、忽略重复内容；
- 导入进度、取消和失败恢复；
- 原文件保存在本地导入目录。

### 4.4 AI 资料检查

- 对用户已有内容给出可选完善建议；
- 建议可以定位到具体记录或字段；
- 支持忽略；
- 没有职责、技能、成果或数字时不得阻断；
- 完全空记录在生成前提示修改或删除；
- AI 不直接修改资料。

退出条件：PRD 26.4 和 26.5 的验收样本全部通过。

## 10. 阶段 5：岗位分析与证据匹配

### 5.1 岗位要求 Schema

定义并版本化：

- 岗位基本信息；
- 职责；
- 必备技能；
- 加分技能；
- 经验或教育要求；
- 每条要求的原文范围和摘要。

### 5.2 证据匹配服务

- 候选检索只读取用户允许的资料；
- 每个匹配保存 `requirement_id` 与 `profile_entry_id`；
- 输出充分匹配、部分匹配、缺少证据、不相关；
- 每个结论包含可解释理由；
- 禁止只输出没有证据链接的笼统匹配分数；
- 资料变化后标记匹配报告过期并允许重新分析。

### 5.3 匹配报告页面

- 岗位原文和匹配报告切换；
- 按要求类型分组；
- 显示证据、缺失信息和建议；
- 前往个人资料补充；
- 本次简历不强调；
- 重新匹配；
- 进入简历配置。

### 5.4 任务状态

- 保存分析步骤和进度；
- 支持取消；
- 网络失败可重试；
- 失败不删除 JD；
- 同一岗位避免重复并发分析。

退出条件：至少 20 组中文 JD 与资料样例完成评测；每条匹配结论均可追溯到证据或明确标记缺少证据。

## 11. 阶段 6：简历配置、生成与工作台

### 6.1 简历配置

- 一页/两页；
- 简洁单栏/技术型双栏；
- 栏目勾选；
- 拖动排序；
- 双栏左右位置；
- 每段经历：必须使用、不使用、AI 决定；
- 配置按岗位独立保存；
- 选择空栏目时提示补充或取消。

### 6.2 写作策略

将以下策略建成版本化规则配置，而不是散落在页面代码中：

- STAR；
- CAR；
- 成果优先；
- 技术岗位；
- 应届生；
- 简洁表达；
- JD 关键词；
- 量化表达；
- ATS 友好。

每个策略包含适用栏目、提示词片段、禁止项和测试样例。没有真实数字时，量化策略只能提示用户补充，不得自行生成数字。

### 6.3 生成编排器

实现可恢复的状态机：

```text
VALIDATE_INPUT
→ BUILD_REDACTED_CONTEXT
→ SELECT_EVIDENCE
→ GENERATE_SECTIONS
→ DETERMINISTIC_FACT_CHECK
→ MODEL_REVIEW
→ COVERAGE_CHECK
→ LAYOUT_CHECK
→ SAVE_DRAFT
```

每一步保存状态、输入摘要、输出引用和错误，不保存模型隐藏推理。

### 6.4 隐私脱敏

- 本地建立随机占位符映射；
- 姓名、电话、邮箱、身份证、照片和自定义敏感字段不进入模型请求；
- 模型返回后只在本地填回；
- 请求日志只记录请求 ID、模型、耗时、Token 用量和结果状态；
- 建立中英文姓名、电话、邮箱和身份证的回归测试集。

### 6.5 事实检查

确定性检查至少覆盖：

- 公司名；
- 职位名；
- 学校名；
- 时间；
- 数字和百分比；
- 技术名词；
- “必须使用”和“本次不使用”约束；
- 占位符；
- 每条内容的 `source_entry_ids`。

模型复核只能补充风险提示，不能替代确定性检查。

### 6.6 工作台

- 左栏：栏目、排序、左右栏和经历取舍；
- 中栏：简历画布、选择段落、直接编辑、证据查看；
- 右栏：AI 修改助手占位；
- 顶栏：返回、模板、页数、撤销、重做、报告、版本、保存、导出；
- 草稿自动保存；
- 本地撤销/重做栈；
- 未确认风险在画布中明显标记。

### 6.7 布局检查

- HTML 预览使用固定 A4 打印尺寸；
- 单栏、双栏共享 `ResumeDocument` 数据；
- 根据一页/两页限制检测溢出；
- 溢出时要求删减内容或切换两页，不能仅缩小字体；
- 页面检查结果保存到草稿状态。

退出条件：两模板、两种页数、栏目排序、经历取舍、写作策略和事实检查全部可用，PRD 26.8、26.9 通过。

## 12. 阶段 7：AI 定向修改与语音

### 7.1 段落选择

- 每个简历块使用稳定 ID；
- 用户只能选择可编辑文本块；
- AI 请求包含目标块、必要上下文、当前 JD 和证据，不发送无关隐私；
- 默认禁止修改未选中块。

### 7.2 修改建议

- 生成修改前、修改后、理由、证据引用；
- 检查新增公司、时间、数字和技能；
- 未接受前不修改草稿；
- 接受后只更新目标块；
- 拒绝后保留原文；
- 重新生成保留用户原指令；
- 支持“只用于当前简历”和“同时保存到资料库”。

### 7.3 文字指令

- 支持简洁、职责、难点、降夸张、JD 关联、STAR/CAR、专业表达；
- 明确拒绝主动生成虚假经历；
- 对完全无证据的要求输出安全替代建议。

### 7.4 语音

- 录音前显示权限状态；
- 录制完成后生成本地临时文件；
- 调用转写供应商适配器；
- 显示文字供用户修改和确认；
- 用户确认后才进入修改流程；
- 成功后删除临时音频；
- 失败、取消或关闭时也要清理过期临时文件；
- 文字输入始终可用。

退出条件：PRD 26.10、26.11 全部通过，且修改目标块之外的内容哈希不变。

## 13. 阶段 8：历史版本、导出、备份与恢复

### 8.1 历史版本

- 普通修改只保存草稿；
- “保存版本”和“保存并导出”创建不可变快照；
- 版本名称、备注、模板、页数、策略和完整内容；
- 查看、改名、改备注、对比、恢复、导出、删除；
- 历史版本只读；
- 有未保存草稿时恢复提供保存、放弃、取消。

### 8.2 版本对比

- 使用稳定块 ID 做结构级对比；
- 显示新增、删除、修改；
- 第一版不做逐字合并；
- 对比不修改任何版本数据。

### 8.3 Word 导出

- 由 `ResumeDocument` 生成 `.docx`；
- 支持单栏和双栏；
- 支持照片、链接、中文字体和分页；
- 填回本地隐私字段；
- 文件名安全化；
- 文件被占用、路径不可写和磁盘不足时给出明确错误。

### 8.4 PDF 导出

- 复用中间 HTML 和打印 CSS；
- 由 Electron 的打印能力生成 PDF；
- 校验页数；
- 生成后验证文件存在且可打开；
- PDF 与预览内容字段一致。

### 8.5 导出前检查

- 占位符；
- 空栏目；
- 未确认 AI 建议；
- 事实风险；
- 页面溢出；
- 必须使用的经历；
- 不用于本次的经历。

### 8.6 备份

- 定义带版本号的备份格式；
- 使用 ZIP 容器和 `manifest.json`；
- 清单包含数据库 Schema 版本、文件列表、大小和 SHA-256；
- 备份 SQLite 使用一致性快照；
- 包含资料、照片、源文件、岗位、草稿、版本和非敏感设置；
- 明确排除 API Key、日志和临时文件；
- 导出前提示备份未加密且包含敏感数据。

### 8.7 恢复

- 在临时目录验证备份格式、版本、哈希和路径；
- 防止 ZIP 路径穿越和压缩炸弹；
- 恢复前自动创建当前数据备份；
- 自动备份成功后才允许恢复；
- 使用数据库事务和目录原子替换；
- 恢复失败时回滚；
- 成功后显示恢复数量和自动备份位置。

### 8.8 清除全部数据

- 要求输入“清除全部数据”；
- 可选择是否删除 Credential Manager 中的 API Key；
- 清除数据库、照片、源文件、草稿、版本和导出记录；
- 不删除用户自行导出到其他目录的 Word/PDF；
- 操作完成后回到首次启动或空首页。

退出条件：PRD 26.12～26.14 全部通过。

## 14. 阶段 9：稳定性、安装包和最终验收

### 9.1 错误与恢复

- API 超时、无网络、Key 无效、余额不足、模型不可用；
- AI JSON 无效和部分栏目失败；
- 文件损坏、扫描 PDF、加密文件；
- 路径不可写、文件被占用、磁盘不足；
- 后端意外退出和自动重启；
- AI 运行、导出、备份、恢复时关闭应用；
- 所有错误确认不会丢失资料、JD 和草稿。

### 9.2 性能目标

- 冷启动后 5 秒内显示可操作窗口，后端较慢时显示明确启动状态；
- 普通本地页面操作 200ms 内反馈；
- 自动保存采用防抖，停止输入后 1 秒左右落盘；
- 100 条资料、50 个岗位、200 个版本下主要列表仍可正常使用；
- 长任务可取消，UI 不冻结；
- 临时文件在正常退出和下次启动时清理。

### 9.3 安装包

- 固定应用 ID、名称和数据目录；
- 使用安装器创建开始菜单和桌面快捷方式；
- 安装器不要求用户安装开发环境；
- 卸载默认不删除用户数据，提供明确选项；
- 覆盖安装执行数据库迁移并保留数据；
- 第一版未签名，文档说明 SmartScreen 提示；
- 生成 SHA-256 校验值供下载页展示。

### 9.4 Windows 测试矩阵

至少覆盖：

- Windows 10 最新可用更新，普通用户权限；
- Windows 11 最新可用更新，普通用户权限；
- 无 Python、无 Node 的干净环境；
- 中文用户名和包含空格的路径；
- 无麦克风或拒绝麦克风权限；
- 无网络启动；
- 覆盖安装；
- 卸载后重装；
- 数据目录只读或磁盘空间不足模拟。

### 9.5 最终验收

- 按 PRD 26.1～26.15 建立验收用例编号；
- 每条用例记录环境、步骤、期望、实际和证据；
- 阻断级和严重级缺陷为 0；
- 中等级缺陷有明确延期说明才可发布；
- 生成 `docs/v1-acceptance-report.md`；
- 生成安装说明、隐私说明、API Key 配置说明和已知限制。

退出条件：完整验收报告通过，两台干净 Windows 测试机完成安装到导出的端到端流程。

## 15. AI 评测计划

### 15.1 测试数据

全部使用合成或明确授权的脱敏数据，至少建立：

- 20 份用户资料；
- 30 份中文岗位 JD；
- 5 份可选择文字的 PDF 简历；
- 5 份 DOCX 简历；
- 10 组事实陷阱样例；
- 10 组缺少工作经历或量化数据的样例；
- 10 组多岗位经历取舍样例；
- 20 条段落修改指令；
- 10 条要求编造经历的对抗指令。

### 15.2 核心指标

| 指标 | V1 门槛 |
|---|---:|
| 结构化输出一次成功率 | ≥ 95% |
| 允许一次重试后的结构化成功率 | ≥ 98% |
| 公司、职位、时间、数字硬事实编造 | 0 |
| 生成块证据引用覆盖率 | 100% |
| “本次不使用”经历误用 | 0 |
| “必须使用”经历遗漏 | 0 |
| PII 泄漏到模型请求的回归测试 | 0 |
| 修改非目标段落 | 0 |
| 空选填栏目被强制补齐 | 0 |

### 15.3 回归机制

- Prompt、Schema、模型或规则变更必须运行黄金测试集；
- 保存 Prompt 版本和评测结果，不保存隐藏推理；
- 真实 API 评测单独运行，普通 CI 使用固定 mock；
- 成本、Token、延迟记录到评测报告；
- 未达到门槛不得合并相关 AI 变更。

## 16. 测试分层

### 16.1 后端单元测试

- 领域规则；
- PII 脱敏和回填；
- 事实白名单检查；
- 栏目和经历取舍；
- 版本不可变规则；
- 备份 manifest 和路径验证；
- 文件名和路径安全。

### 16.2 前端单元与组件测试

- 选填字段；
- 导入确认；
- 栏目拖动和双栏位置；
- 修改前后对比；
- 危险操作确认；
- 空、加载和失败状态；
- API Key 掩码。

### 16.3 集成测试

- SQLite 迁移；
- API 路由与数据库；
- 文档解析；
- Mock AI 工作流；
- DOCX/PDF 生成；
- 备份和恢复；
- Credential Manager 封装。

### 16.4 E2E 测试

至少自动化：

1. 首次启动→配置 Key mock→手填资料→创建岗位→生成草稿；
2. 导入 DOCX→确认归类→岗位匹配→生成；
3. 修改段落→拒绝→再次修改→接受；
4. 保存版本→继续编辑→对比→恢复；
5. 导出 Word/PDF；
6. 备份→修改数据→恢复；
7. 清除全部数据；
8. API 失败后重试；
9. 覆盖安装后数据保留。

### 16.5 手工探索测试

- 极长中文文本；
- 特殊字符、Emoji、中英文混排；
- 多显示器和缩放 100%/125%/150%；
- 快速连续保存和取消；
- AI 运行时切换岗位；
- 断网、休眠、恢复；
- Word 文件占用时导出；
- 中文用户名和长路径。

## 17. 里程碑与演示内容

### M0：技术可行性通过

演示：干净 Windows 安装最小包，录音转写，解析 PDF/DOCX，导出四类简历样本。

### M1：本地应用基础完成

演示：首次启动、API Key、个人资料、本地保存和重启恢复。

### M2：最小 AI 简历闭环

演示：手填资料、粘贴 JD、分析并生成单栏简历草稿。

### M3：资料导入和岗位匹配

演示：导入 PDF/DOCX、确认归类、查看证据匹配报告。

### M4：完整简历工作台

演示：栏目排序、双栏、写作策略、事实检查、定向修改。

### M5：可交付本地产品

演示：语音修改、版本、Word/PDF、备份恢复和安装包。

### M6：V1 发布候选版

演示：两台干净 Windows 机器完成安装到导出的完整流程，展示验收报告。

## 18. 可直接执行的首批任务

以下任务按顺序执行。没有完成前一项的退出条件，不进入后一项。

### 第 1 批：文档和风险样本

- [ ] 创建 `docs/architecture-spike.md` 模板；
- [ ] 准备 5 份合成 PDF、5 份合成 DOCX 和 20 组 AI 事实测试数据；
- [ ] 明确两套模板的纸张尺寸、字体、字号和页边距；
- [ ] 定义 `ParsedDocument`、`ResumeDocument`、岗位分析和证据匹配 JSON Schema；
- [ ] 建立隐私字段与占位符清单；
- [ ] 建立 PRD 26 章到测试编号的追踪表。

### 第 2 批：技术 Spike

- [ ] 创建 Electron + React 最小窗口；
- [ ] 创建 FastAPI `/health`；
- [ ] PyInstaller 打包 Python 后端；
- [ ] Electron 启停后端并处理残留进程；
- [ ] 生成第一个 Windows 安装包；
- [ ] 在干净测试机验证；
- [ ] 验证麦克风录音和在线转写；
- [ ] 验证 PDF/DOCX 读取；
- [ ] 验证 HTML/PDF/DOCX 四类导出样本；
- [ ] 验证 OpenAI 结构化输出和事实检查。

### 第 3 批：正式工程骨架

- [ ] 建立正式目录和统一脚本；
- [ ] 配置 lint、format、unit、integration、e2e；
- [ ] 建立 Electron 安全 preload；
- [ ] 建立 FastAPI 会话令牌和随机端口；
- [ ] 建立 OpenAPI TS 客户端；
- [ ] 建立全局错误和日志脱敏；
- [ ] 建立 SQLite 迁移系统；
- [ ] 建立 AppData 目录服务；
- [ ] 建立 Windows Credential Manager 封装。

第 3 批完成后，按阶段 2～9 顺序推进。

## 19. 风险清单和应对

| 风险 | 影响 | 提前措施 | 失败时降级 |
|---|---|---|---|
| Electron + Python 打包后端失败 | 无法发布安装包 | 阶段 0 首先验证 | 改用单体 Python 桌面壳，但保持 API/领域层 |
| DOCX 与 PDF 排版不一致 | 导出不可用 | 先定义中间文档模型和四类样本 | 明确 DOCX 为可编辑版、PDF 为视觉基准 |
| 一页/两页分页不稳定 | 验收失败 | 固定字体、纸张、打印 CSS，提前布局测试 | 阻止导出并提示用户删减或切换两页 |
| AI 编造硬事实 | 产品底线失守 | 证据 ID、确定性白名单、黄金测试 | 阻止保存和导出风险内容 |
| 文档格式差异过大 | 导入效果不稳定 | 5+5 样本、候选结果确认 | 显示原文并允许手动归类 |
| API 变化或模型不可用 | AI 功能中断 | Provider 接口、模型配置、Schema 重试 | 允许切换可用模型，保留本地数据 |
| API Key 泄漏 | 严重安全问题 | Credential Manager、日志脱敏、扫描 | 立即阻断发布并轮换测试 Key |
| 备份恢复破坏数据 | 用户数据丢失 | 校验、自动备份、事务和原子替换 | 回滚恢复前自动备份 |
| 安装包被 SmartScreen 警告 | 用户信任下降 | 明确说明、发布校验值 | 后续购买签名证书或商店发布 |
| 安装包过大 | 下载体验差 | 记录体积基线、移除无用资源 | 接受 V1 较大体积，V2 优化 |

## 20. 发布门槛

只有满足以下条件才可标记 V1.0：

- [ ] PRD 26.1～26.15 全部有测试记录；
- [ ] 阻断级和严重级缺陷为 0；
- [ ] AI 黄金测试达到第 15.2 节门槛；
- [ ] PII 泄漏回归测试为 0；
- [ ] 两台干净 Windows 测试机端到端通过；
- [ ] 安装、覆盖安装、卸载和重装通过；
- [ ] Word/PDF 四种模板页数组合通过；
- [ ] 备份、破坏后恢复和失败回滚通过；
- [ ] API Key 未出现在数据库、备份、日志和安装包配置中；
- [ ] 用户手册、隐私说明、安装说明和已知限制完成；
- [ ] 安装包 SHA-256 已生成并记录；
- [ ] `docs/v1-acceptance-report.md` 完成。

## 21. 计划维护规则

- PRD 变更时同步更新本计划的范围、依赖和验收映射；
- 每个阶段开始前将任务拆成不超过 1～2 天的开发项；
- 每周更新完成项、风险、实际用时和下一周目标；
- 新功能不得口头加入 V1，必须先更新 PRD；
- 高风险技术结论必须记录到 `docs/architecture-spike.md` 或 ADR；
- 模型、Prompt、Schema 和数据库迁移都必须版本化；
- 延期功能必须移入明确的 V2 列表，不能以隐藏半成功状态留在 V1。

