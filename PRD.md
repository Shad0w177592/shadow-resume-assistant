# 影子简历助手 PRD

> 文档状态：第一版需求与页面交互已确认  
> 产品版本：V1.0  
> 目标平台：Windows 10 / Windows 11  
> 更新日期：2026-08-22  
> 用途：作为后续视觉设计、技术架构、任务拆分、开发和验收的统一依据

## 1. 产品概述

### 1.1 产品名称

产品正式名称为“影子简历助手”。

所有用户可见位置统一使用该名称，包括：

- 安装程序名称；
- Windows 桌面快捷方式；
- 软件窗口标题；
- 左上角品牌名称；
- 设置与关于页面；
- 默认本地数据目录名称。

内部工程英文名可暂定为 `Shadow Resume Assistant`。

### 1.2 产品定位

影子简历助手是一款面向应届生和有工作经验求职者的 Windows 本地 AI 简历生成与修改工具。

用户可以手动填写个人资料，也可以导入已有 PDF 或 Word 简历。用户为每个目标岗位粘贴 JD（职位描述），系统在不编造经历的前提下，分析岗位要求、匹配用户的真实经历、生成一份针对该岗位的中文简历，并允许用户通过文字或语音对指定段落提出修改要求。

产品前台采用表单、配置页和简历编辑器；后台采用受控的多步骤 AI 工作流，而不是仅向模型发送一句“帮我写简历”。

### 1.3 产品目标

第一版需要实现以下目标：

1. 帮助用户建立一份可重复使用的个人求职资料库；
2. 将已有 PDF、Word 简历中的内容提取到结构化表单；
3. 分析不同目标岗位的职责和要求；
4. 从用户真实经历中选择与岗位最相关的证据；
5. 按用户选择的栏目、顺序、模板、长度和写作策略生成中文简历；
6. 支持用户对指定段落进行受控的 AI 修改；
7. 对 AI 生成内容进行事实一致性和岗位覆盖检查；
8. 导出可实际投递的 Word 和 PDF 简历；
9. 将个人资料、岗位、草稿和版本安全地保存在用户电脑本地；
10. 形成一个可展示文档解析、结构化输出、AI 工作流、人工确认、版本管理和评测能力的完整 AI 应用作品。

### 1.4 核心使用场景

#### 场景 A：从已有简历开始

1. 用户安装并启动影子简历助手；
2. 用户配置自己的 OpenAI API Key；
3. 用户导入一份可选择文字的 PDF 或 `.docx` 简历；
4. 系统提取内容并填入个人资料表；
5. 用户确认不确定内容，补充项目职责和成果；
6. 用户粘贴某个岗位 JD；
7. 系统生成岗位匹配报告和定制简历；
8. 用户通过文字或语音修改指定段落；
9. 用户保存正式版本并导出 Word、PDF。

#### 场景 B：从空白资料开始

1. 用户在个人资料页填写个人信息、教育、工作、项目、技能等；
2. 用户使用 AI 资料检查补充模糊或缺失信息；
3. 用户创建目标岗位并粘贴 JD；
4. 用户选择简历栏目、顺序、模板、长度和写作策略；
5. 系统生成、检查并展示简历；
6. 用户保存和导出。

#### 场景 C：为多个岗位分别制作简历

1. 用户保留同一份个人资料库；
2. 用户分别创建公司 A、公司 B 的目标岗位；
3. 每个岗位独立保存 JD、经历取舍、栏目配置、草稿和历史版本；
4. 用户无需回到个人信息中修改“求职方向”；
5. 求职方向完全由当前岗位 JD 决定。

#### 场景 D：修改已生成的某一段

1. 用户在简历工作台选中一个段落；
2. 用户输入或录制修改要求；
3. 语音先转成文字并由用户确认；
4. 系统只针对所选段落生成修改建议；
5. 系统展示修改前、修改后、理由和证据来源；
6. 用户接受后才替换原文，拒绝则保持原文。

### 1.5 产品原则

- 真实优先：AI 不主动编造公司、职位、任职时间、工作经历、项目经历或成绩数据；
- 用户确认：重要的 AI 解析、生成和修改结果必须经过用户确认；
- 本地优先：资料、照片、岗位、草稿、版本和导出记录保存在本机；
- 隐私最小化：姓名、电话、邮箱、身份证号等直接身份信息不发送给模型；
- 岗位独立：每个岗位独立保存经历取舍、栏目配置和简历版本；
- 简洁操作：首页聚焦继续任务、新建岗位和完善资料，不做复杂数据看板；
- 失败可恢复：网络、API、解析或导出失败不能导致用户资料和草稿丢失；
- AI 过程透明：用户可以看到岗位要求、匹配证据、缺少证据和内容取舍原因。

## 2. 第一版运行与发布方式

### 2.1 运行方式

- 第一版为 Windows 本地桌面程序；
- 提供可下载安装的安装包；
- 用户安装后通过桌面图标启动；
- 用户不需要额外安装 Python 或 Node.js；
- 不需要注册、登录或云端账号；
- 程序运行和数据存储均在本机；
- 调用大模型和语音转写时需要联网。

### 2.2 安装包限制

- 第一版安装包暂不做正式代码签名；
- 安装时可能出现“未知发布者”或 SmartScreen 提示；
- 第一版不做自动更新；
- 新版本由用户手动下载安装；
- 覆盖安装不得主动删除原有本地数据。

### 2.3 技术方向约束

已确认的技术方向为：

- React：桌面界面；
- Python FastAPI：本地业务服务、AI 工作流和文件处理；
- SQLite：本地结构化数据；
- Windows 安装包：将运行环境与程序一起打包；
- 第一版使用 OpenAI 文本模型和 OpenAI 在线语音转写；
- 模型调用层需要预留供应商适配接口，第二版可接入 DeepSeek。

技术实现可在架构设计阶段调整细节，但不能改变本 PRD 中的用户体验和数据边界。

## 3. 用户与数据范围

### 3.1 用户范围

- 第一版同时面向应届生和有工作经验的求职者；
- 第一版每个本地安装实例只保存一位求职者的个人资料；
- 同一位求职者可以保存多个目标岗位和多个简历版本；
- 第一版只生成中文简历。

### 3.2 个人资料栏目

个人资料库提供以下可选栏目：

- 个人信息与照片；
- 教育经历；
- 工作经历；
- 实习经历；
- 项目经历；
- 校园、社团及志愿经历；
- 专业技能；
- 证书与奖项；
- 其他自定义经历和成果。

上述栏目均为选填。用户没有某类经历时可以直接留空，不需要专门填写“无”；空栏目不影响个人资料保存、岗位分析或简历生成，也不会自动出现在最终简历中。

系统可以在界面中显示“选填”或“没有可留空”的提示，但不得要求用户为了提高完整度而填写不存在的经历。个人照片同样为选填。

个人信息不包含固定的“当前求职方向”。求职方向由每个目标岗位的 JD 决定。

### 3.3 其他经历

用户可以新增任意数量的“其他经历”。该栏目采用灵活录入方式，不要求用户按照固定字段清单填写。

用户可以只填写自己实际拥有的信息，例如一段自由文本、一个标题，或者时间、组织、职责、技能、成果中的任意组合。系统只分析用户实际提供的内容，不得因为用户没有填写技能、成果、量化数据等信息而阻止保存或要求补齐。

界面可以提供标题、时间、所属组织、经历内容等便于整理的可选字段，也可以提供自由文本输入；这些字段均不是必填项。AI 可以提示哪些补充信息可能让简历表达更完整，但提示必须是可忽略的建议。

## 4. 整体页面框架与导航

### 4.1 主导航

左侧固定主导航只保留四项：

1. 首页；
2. 个人资料；
3. 目标岗位；
4. 设置。

“简历工作台”“岗位匹配报告”和“历史版本”从具体目标岗位进入，不单独占用主导航。

### 4.2 核心用户流程

```text
首次启动与 API 配置
        ↓
建立或导入个人资料
        ↓
AI 检查并补充资料
        ↓
新建目标岗位并粘贴 JD
        ↓
岗位分析与证据匹配
        ↓
选择栏目、顺序、模板、长度和写作策略
        ↓
生成并自动检查简历
        ↓
文字或语音修改指定段落
        ↓
手动保存历史版本
        ↓
导出 Word / PDF
```

### 4.3 首页、今日计划与各模块关系

当前产品已经从原“AI 日程 App”切换为“影子简历助手”。已确认的影子简历助手第一版中没有“今日计划”模块，也不提供日程、任务或提醒功能。

为避免概念混淆，本产品中的对应关系如下：

- 首页：整个产品的任务入口和状态总览；
- “继续编辑当前简历”：首页最主要的继续任务入口；
- 个人资料：为所有岗位提供可复用的真实素材；
- 目标岗位：保存 JD、匹配结果和当前岗位配置；
- 简历工作台：生成、编辑和检查当前岗位简历；
- 历史版本：保存用户主动确认的正式版本；
- 设置：管理 API、语音、本地数据、备份和清除数据。

如果后续需要“今日计划”，必须作为新需求单独评审，不属于第一版。

## 5. 首页

### 5.1 用途

首页用于让用户快速继续当前任务、新建目标岗位、导入简历、完善资料和打开最近内容。首页不承担资料编辑、岗位详细配置或危险数据操作。

### 5.2 内容结构

首页从上到下包括：

1. 顶部标题和 API 连接状态；
2. 当前简历草稿卡片；
3. 个人资料完整度卡片；
4. “新建目标岗位”和“导入已有简历”两个主要按钮；
5. 最近的目标岗位列表；
6. 最近保存的简历版本；
7. 本地数据保存位置提示。

### 5.3 第一版最必要数据

- API 状态：未配置、连接正常、连接失败；
- 当前草稿对应的公司、岗位、状态和最近编辑时间；
- 个人资料完整度和最多三条关键完善建议；
- 最近岗位的公司、岗位、状态和最近编辑时间；
- 最近版本的版本名、岗位、模板、页数和保存时间；
- 本地数据目录。

首页的“个人资料完整度”只评估用户已经创建的记录是否可正常使用，以及是否仍有未确认的导入内容、空记录或明显冲突。没有填写的选填栏目、没有上传照片、没有成果或量化数据均不得扣减完整度。完善建议必须可以忽略。

### 5.4 第一版最必要操作

- 继续编辑当前草稿；
- 新建目标岗位；
- 导入已有简历；
- 前往完善个人资料；
- 打开最近岗位；
- 查看或导出最近版本；
- 点击 API 状态进入设置。

首页不提供删除岗位、清除数据、恢复备份等危险操作。

## 6. 首次启动模块

### 6.1 用途

帮助用户理解隐私边界、配置 AI 服务并建立第一份个人资料。

### 6.2 页面步骤

1. 欢迎与隐私说明；
2. OpenAI API 配置；
3. 导入简历或从空白资料开始；
4. 初始化完成。

### 6.3 最必要数据

- 用户是否确认隐私说明；
- OpenAI API Key；
- 选定文本模型；
- API 连接测试结果；
- 用户是否已建立或导入个人资料；
- 本地数据目录初始化结果。

### 6.4 最必要操作

- 阅读并确认隐私说明；
- 输入、显示或隐藏 API Key；
- 测试连接；
- 保存配置；
- 暂时跳过 API 配置；
- 导入 PDF、Word 简历；
- 从空白资料开始；
- 暂时跳过资料建立。

跳过 API 配置后可以填写资料，但不能使用 AI 解析、检查、生成或语音转写功能。

## 7. 个人资料模块

### 7.1 用途

保存用户愿意长期保留和复用的真实求职素材。不同岗位从同一资料库中选择不同内容。

用户完全不想保存或公开的经历，应从个人资料库删除；“本次不使用”属于岗位级配置，而不是资料级永久禁用。

个人资料中的所有栏目均为选填。用户没有某类经历时直接留空即可，系统不得要求填写“无”，也不得因空栏目阻止后续流程。

### 7.2 个人信息数据

以下个人信息字段均为选填：

- 姓名；
- 照片；
- 手机号；
- 邮箱；
- 所在城市；
- GitHub、个人网站或作品集链接；
- 用户自定义基本信息字段。

未填写的个人信息不进入最终简历。系统可以在导出前提醒联系信息可能不完整，但不得阻止用户保存草稿或生成简历。

### 7.3 经历类数据

工作、实习、项目、校园和其他经历可以使用以下可选信息组织内容：

- 名称；
- 开始和结束时间；
- 所属组织；
- 个人角色；
- 背景或目标；
- 用户具体负责的工作；
- 使用的技术或技能；
- 技术难点与解决方法；
- 最终成果；
- 可量化数据；
- 其他补充。

除系统识别一条记录所需的最小内容外，上述信息均为可选。用户有多少真实信息就填写多少，系统有多少信息就分析多少。

AI 资料检查可以指出哪些可选信息可能改善简历表达，但不得把职责、技术、成果或量化数据设为必填项，也不得因为用户忽略建议而阻止保存、岗位分析或简历生成。没有相关信息时，AI 应基于现有内容生成，不能补写或猜测不存在的事实。

### 7.4 最必要操作

- 新增、编辑、复制、删除经历；
- 更换个人照片；
- 新增自定义字段和其他经历；
- 导入简历或作品集；
- 查看导入结果；
- 确认、改分类、忽略或合并导入内容；
- 执行 AI 资料检查；
- 根据建议定位并补充字段；
- 保存资料。

### 7.5 自动保存

- 表单编辑过程自动保存为本地草稿；
- 页面显示最近自动保存状态；
- 用户可以点击“保存并返回”明确结束本次编辑；
- 自动保存不创建简历历史版本。

## 8. 文件导入模块

### 8.1 支持格式

- 简历：可选择文字的 PDF、`.docx`；
- 作品集或项目说明：PDF、`.docx`、Markdown、纯文本；
- 第一版不支持扫描件、纯图片 PDF 和 OCR；
- 第一版不支持自动扫描完整项目目录、Git 仓库或大量源代码。

### 8.2 处理流程

```text
选择文件 → 本地读取 → AI 识别和归类 → 用户确认 → 写入个人资料
```

### 8.3 导入结果状态

- 已明确识别；
- AI 不确定，需要确认；
- 无法归类；
- 可能与现有资料重复。

### 8.4 必要操作

- 确认写入；
- 修改归类；
- 忽略；
- 合并重复经历；
- 保留两条；
- 用新内容替换；
- 文件失败后重新选择或手动填写。

导入不得在未确认时直接覆盖现有资料。

## 9. AI 资料检查模块

### 9.1 用途

在生成简历前找出模糊、缺失、冲突或无法支持岗位表达的资料。

### 9.2 检查内容

- 是否有可以进一步说明的具体职责；
- 是否有可以进一步说明的行动和解决方法；
- 是否有可以补充的最终成果；
- 是否有用户实际掌握且愿意补充的量化信息；
- 时间是否存在明显冲突；
- 是否存在重复内容；
- 技能是否可以关联已有使用场景；
- 导入内容是否仍有未确认项。

前四类均属于可选完善建议，不是资料完整性的硬性要求。用户没有相关信息或选择不补充时，系统应继续使用现有内容。

### 9.3 必要操作

- 检查全部资料；
- 检查单条经历；
- 前往对应字段补充；
- 关联技能和经历；
- 忽略非必要建议。

AI 只能给出建议，不得未经用户确认直接修改个人资料。

## 10. 目标岗位模块

### 10.1 用途

保存多个目标岗位，并为每个岗位独立管理 JD、匹配报告、栏目配置、当前草稿和历史版本。

### 10.2 最必要数据

- 公司名称；
- 岗位名称；
- JD 原文；
- 用户备注；
- 创建和最近编辑时间；
- 岗位状态；
- 岗位要求结构化结果；
- 证据匹配结果；
- 当前简历配置；
- 当前草稿；
- 历史版本列表。

公司名称和岗位名称可以手动填写，也可以由 AI 从 JD 中提取；提取不到时要求用户补充。

### 10.3 岗位状态

- 尚未分析；
- 等待补充资料；
- 尚未生成；
- 正在编辑；
- 有未保存修改；
- 已保存版本。

### 10.4 必要操作

- 新建岗位；
- 粘贴和修改 JD；
- 保存岗位草稿；
- 分析岗位；
- 查看岗位原文和匹配报告；
- 打开或继续编辑简历；
- 复制岗位；
- 修改岗位信息；
- 查看历史版本；
- 删除岗位并二次确认。

第一版不自动读取 BOSS、智联等招聘网站链接。

## 11. 岗位分析与匹配模块

### 11.1 后台步骤

1. 识别岗位基本信息；
2. 提取岗位职责；
3. 提取必备技能；
4. 提取加分技能；
5. 从个人资料中检索对应经历；
6. 建立岗位要求与真实证据之间的关系；
7. 生成匹配报告。

### 11.2 匹配等级

第一版不用容易误导的百分制，只使用：

- 充分匹配；
- 部分匹配；
- 缺少证据；
- 不相关。

### 11.3 匹配报告数据

每条岗位要求至少显示：

- 岗位要求原文或摘要；
- 要求类型；
- 匹配等级；
- 对应个人经历；
- 对应证据说明；
- 缺少的具体信息；
- 是否建议写入当前简历。

### 11.4 必要操作

- 返回修改 JD；
- 前往个人资料补充证据；
- 重新执行匹配；
- 将某项设为本次简历不强调；
- 进入简历配置。

## 12. 简历配置模块

### 12.1 用途

让用户在生成前明确控制简历长度、模板、栏目、顺序、经历取舍和写作策略。

### 12.2 长度选择

- 一页精简版；
- 两页详细版。

AI 需要根据所选长度筛选内容，不能简单缩小字体或强行塞入所有经历。

### 12.3 模板选择

- 简洁单栏；
- 技术型双栏。

双栏模板允许用户把栏目拖到左栏或右栏，并分别调整两栏顺序。

### 12.4 栏目选择

所有栏目都可以勾选或取消，默认勾选：

- 个人信息；
- 工作经历；
- 专业技能；
- 自我介绍。

其他栏目包括：

- 教育经历；
- 实习经历；
- 项目经历；
- 校园、社团及志愿经历；
- 证书与奖项；
- 其他自定义栏目。

如果用户勾选了没有任何资料的栏目，程序必须提示补充或取消，不能让 AI 自动编写内容。

### 12.5 当前岗位经历取舍

每段经历在当前岗位中可以设置：

- 必须用于这份简历；
- 不要用于这份简历；
- 由 AI 根据岗位自动决定。

这些选择只影响当前岗位，不影响其他岗位。

### 12.6 内置写作策略

第一版支持：

- STAR；
- CAR；
- 成果优先；
- 技术岗位风格；
- 应届生风格；
- 简洁表达；
- JD 关键词匹配；
- 量化表达；
- ATS 友好。

用户可以为整份简历选择主要策略，也可以对工作经历或项目经历单独选择策略。

STAR、CAR 等规则用于后台组织素材，最终输出仍应是自然、简洁的简历内容，不机械显示“情境、任务、行动、结果”等标签。

所有策略都必须遵守真实性规则，没有真实数字时不得生成数字。

## 13. 简历生成与检查模块

### 13.1 后台工作流

```text
读取用户配置
→ 分析岗位
→ 检索真实经历
→ 选择内容
→ 组织栏目与顺序
→ 按写作策略生成
→ 事实一致性检查
→ 岗位覆盖检查
→ 页面长度检查
→ 展示给用户
```

### 13.2 生成前检查

- 是否存在空栏目；
- 是否存在未确认的导入资料；
- 是否选择了无证据的能力；
- 是否存在完全没有可用内容的经历记录；
- 模板、长度和写作策略是否已选择；
- 将发送给 AI 的资料范围。

经历没有填写技能、成果或量化数据不属于阻断条件。只有记录完全没有可用于生成简历的内容时，系统才提示用户修改或移除该空记录。

### 13.3 生成后检查

- 是否出现个人资料中不存在的公司、职位或时间；
- 是否出现无证据的成绩数字；
- 是否存在明显夸大；
- 是否遗漏用户设为“必须使用”的经历；
- 是否错误使用“不要用于当前简历”的经历；
- 是否覆盖岗位核心要求；
- 是否超过一页或两页限制；
- 是否出现占位符或空栏目。

存在事实风险的内容必须被标记，在用户处理前不能保存为正式版本。

## 14. 简历工作台

### 14.1 页面结构

简历工作台采用三栏布局：

- 左侧：栏目、顺序、显示状态和经历取舍；
- 中间：接近最终导出效果的简历预览与编辑；
- 右侧：AI 修改助手。

顶部工具栏包括：

- 返回岗位；
- 当前模板；
- 一页或两页；
- 撤销；
- 重做；
- 岗位匹配报告；
- 历史版本；
- 保存版本；
- 保存并导出。

### 14.2 中间编辑区

必须支持：

- 实时预览简历；
- 直接修改文字；
- 点击或选中某个段落；
- 查看段落对应的资料证据；
- 对无事实依据的内容显示风险标记；
- 自动保存当前草稿；
- 撤销和重做当前编辑操作。

### 14.3 左侧栏目区

必须支持：

- 显示或隐藏栏目；
- 拖动调整顺序；
- 双栏模板中调整左右位置；
- 展开查看栏目内经历；
- 调整当前岗位的经历取舍。

## 15. AI 文字与语音修改模块

### 15.1 用途

允许用户对当前选中的段落提出修改要求，而不是重新生成整份简历。

### 15.2 文字修改

用户可以提出：

- 写得更简洁；
- 突出个人职责；
- 强调技术难点；
- 降低夸张程度；
- 强化与 JD 的联系；
- 改为 STAR、CAR 或成果优先表达；
- 保留原意但改得更专业。

系统只能修改用户选中的内容，除非用户明确要求调整整份简历。

### 15.3 语音修改流程

```text
用户主动录音
→ 上传至在线语音转写
→ 显示转写文字
→ 用户确认或修改文字
→ 发送修改要求
→ 生成修改建议
```

转写成功后默认删除临时录音，只保存用户确认后的文字指令。

### 15.4 修改建议数据

- 当前选中段落；
- 用户指令；
- 修改前文本；
- 修改后文本；
- 修改理由；
- 使用的真实证据；
- 是否包含新增事实；
- 接受或拒绝状态。

### 15.5 必要操作

- 输入文字；
- 开始和停止录音；
- 确认转写文字；
- 重新录音；
- 生成修改建议；
- 接受；
- 拒绝；
- 重新生成；
- 将新增真实资料选择为“只用于当前简历”或“同时保存到个人资料库”。

修改建议未被接受前不得替换原文。

## 16. 真实性与风险控制

### 16.1 不可关闭的规则

- AI 不主动生成虚假工作经历；
- AI 不主动生成虚假项目经历；
- AI 不主动生成虚假公司、职位、任职时间；
- AI 不主动生成用户未提供的成绩数字；
- AI 不把尚未完成的项目写成已完成经历；
- AI 不以“用户坚持”为由关闭上述规则。

### 16.2 允许的行为

- 优化用户自行填写的内容；
- 将用户内容标记为“用户提供、未验证”；
- 把校园、个人项目、志愿经历转化为与岗位相关的真实能力表达；
- 生成带明显占位符的写作框架；
- 生成明确标注为“尚未完成”的项目建议；
- 提醒用户对正式导出的简历真实性负责。

正式导出前必须检查未替换占位符和未确认风险内容。

## 17. 历史版本模块

### 17.1 版本创建规则

- 普通逐段修改只更新当前草稿，不创建历史版本；
- 只有用户点击“保存版本”或“保存并导出”时才创建历史版本；
- 历史版本只读，不能被悄悄修改；
- 要修改历史版本，必须先恢复为当前草稿。

### 17.2 版本数据

- 版本 ID；
- 所属目标岗位；
- 版本名称；
- 保存时间；
- 用户备注；
- 完整简历内容；
- 模板；
- 一页或两页；
- 栏目选择和顺序；
- 写作策略；
- 事实检查状态。

### 17.3 必要操作

- 保存新版本；
- 查看只读版本；
- 修改版本名称和备注；
- 与当前草稿对比；
- 恢复为当前草稿；
- 导出；
- 删除并二次确认。

第一版版本对比只显示新增、删除和修改内容，不支持复杂的逐字合并。

如果当前草稿有未保存修改，恢复历史版本前必须让用户选择：

- 取消；
- 放弃当前修改并恢复；
- 先保存当前草稿为新版本，再恢复。

## 18. 导出模块

### 18.1 支持格式

- Word；
- PDF；
- Word 和 PDF 同时导出。

### 18.2 必要操作

- 选择格式；
- 选择保存位置；
- 修改文件名称；
- 导出；
- 导出成功后打开文件；
- 打开所在文件夹；
- 导出失败后更换位置或重试。

### 18.3 导出前检查

- 是否存在占位符；
- 是否存在空栏目；
- 是否存在未确认的 AI 内容；
- 是否存在事实风险；
- 是否符合所选页面长度；
- 是否有用户设为必须使用但未出现的经历。

### 18.4 模板边界

- 第一版只提供简洁单栏和技术型双栏两套模板；
- 第一版不保证保留用户原始 Word 或 PDF 的排版；
- 第一版不支持任意自定义 Word 模板；
- 第二版可考虑支持带占位符的自定义 Word 模板。

## 19. 设置模块

### 19.1 AI 服务

最必要数据和操作：

- 模型供应商：第一版仅 OpenAI；
- API Key；
- 文本模型；
- 显示或隐藏 Key；
- 测试连接；
- 保存设置；
- 查看最近一次连接结果；
- 查看费用提示。

修改 API Key 时，新配置测试成功后才替换原来的有效配置；测试失败需要保留旧配置。

API Key 不得写入程序代码、普通数据库、日志、备份或可提交到代码仓库的配置文件。

### 19.2 语音输入

- 选择或显示当前麦克风；
- 检测麦克风权限；
- 开始测试录音；
- 显示转写结果；
- 默认识别语言为中文；
- 麦克风不可用时提供文字输入降级方案。

### 19.3 数据管理

- 查看本地数据目录；
- 打开数据目录；
- 查看岗位、版本和文件占用概况；
- 导出全部备份；
- 从备份恢复；
- 清除全部本地数据。

### 19.4 软件信息

- 产品名称；
- 当前版本；
- 隐私说明；
- 开源许可证；
- 本地日志目录；
- 第一版不支持自动更新的说明。

## 20. 本地数据文件要求

### 20.1 数据分类

本地至少需要保存：

- 用户个人信息；
- 照片；
- 结构化教育、工作、项目和其他经历；
- 导入的源文件及其解析结果；
- 目标岗位和 JD；
- 岗位要求和证据匹配关系；
- 当前简历配置；
- 当前草稿；
- 用户主动保存的历史版本；
- AI 修改建议的必要状态；
- 导出文件路径记录；
- 非敏感应用设置。

### 20.2 建议数据实体

后续技术设计至少应覆盖：

- `UserProfile`：单一用户资料；
- `ProfileSectionEntry`：教育、工作、项目等资料条目；
- `SourceDocument`：导入文件及解析状态；
- `JobTarget`：目标岗位；
- `JobRequirement`：结构化岗位要求；
- `EvidenceLink`：要求与个人经历的证据关系；
- `ResumeConfig`：栏目、顺序、模板、长度和策略；
- `ResumeDraft`：当前草稿；
- `ResumeVersion`：正式历史版本；
- `EditProposal`：AI 段落修改建议；
- `AppSetting`：非敏感设置；
- `BackupManifest`：备份清单和版本信息。

具体字段和表结构由技术设计决定。

### 20.3 文件目录要求

- 数据保存在当前 Windows 用户的本地应用数据目录；
- 不依赖工作区、桌面或安装目录作为唯一数据位置；
- 覆盖安装不能删除数据目录；
- 照片、导入源文件、导出文件和备份应分目录管理；
- 临时录音和中间文件需要及时清理；
- 日志不得包含完整 API Key、手机号、邮箱或未经脱敏的完整简历正文。

## 21. 隐私要求

### 21.1 不发送给 AI 的信息

- 姓名；
- 手机号；
- 邮箱；
- 身份证号；
- 其他可直接识别用户身份的基本字段；
- 用户照片。

生成时在本地替换为占位符，模型返回后再由程序在本地填回。

### 21.2 可能发送给 AI 的信息

- 用户明确提供的经历正文；
- 技能、职责、项目和成果；
- 目标岗位 JD；
- 用户确认后的文字修改要求；
- 用户主动录制的短语音。

首次启动和设置页必须向用户说明上述边界。

## 22. 备份与恢复要求

### 22.1 备份范围

全部备份至少包含：

- 个人资料和照片；
- 导入的简历及作品集；
- 目标岗位和 JD；
- 岗位分析结果；
- 当前草稿；
- 历史版本；
- 栏目、模板和写作策略配置；
- 非敏感应用设置。

API Key 不进入备份文件。恢复到新电脑后，用户需要重新填写 API Key。

### 22.2 备份格式与提醒

- 第一版备份文件不加密；
- 导出前必须提示备份包含敏感个人信息；
- 备份不得自动上传到云端；
- 用户选择本地保存位置。

### 22.3 恢复规则

- 第一版采用“整包替换”，不合并两份数据；
- 恢复前自动备份当前全部数据；
- 自动备份成功后才能开始替换；
- 恢复失败时保留恢复前数据；
- 恢复完成后显示恢复项目数量和恢复前自动备份位置。

### 22.4 清除数据

清除全部本地数据需要删除：

- 个人资料和照片；
- 导入文件；
- 目标岗位和 JD；
- 草稿；
- 历史版本；
- 导出记录。

执行前要求用户输入指定确认文字。API Key 是否同时删除由用户勾选决定。

## 23. 加载、失败与恢复状态

### 23.1 AI 任务

AI 分析或生成时需要展示明确步骤和当前进度，不只显示无限旋转图标。用户可以取消任务，取消后不得删除已填写内容。

### 23.2 网络或 API 失败

失败提示至少说明：

- 资料和草稿已保存在本机；
- 可能是网络、Key、额度或模型可用性问题；
- 可以检查设置或重新尝试。

### 23.3 AI 输出异常

- 无法解析的结果不得写入正式简历；
- 支持只重新生成出错栏目；
- 事实检查发现无证据表述时，用户可以删除、改为不含数字的表达或回到资料页补充证据。

### 23.4 文件和导出失败

- 文件不可读时提供原因和支持格式；
- 导出失败时保留当前简历和版本；
- 提供更换保存位置和重试操作。

### 23.5 软件关闭

普通关闭不弹窗打扰用户，因为草稿自动保存。以下情况需要提示：

- AI 正在生成；
- 文件正在导出；
- 备份或恢复正在进行；
- 存在尚未接受或拒绝的 AI 修改建议。

## 24. 第一版必须完成的功能

1. Windows 10 / 11 本地安装和启动；
2. 无账号、无登录、无云数据库；
3. 首次启动隐私确认和 OpenAI API 配置；
4. API 连接测试和本机安全保存；
5. 单一用户个人资料库；
6. 个人照片和自定义基本字段；
7. 教育、工作、实习、项目、校园、技能、证书及其他经历管理；
8. 可选择文字的 PDF、`.docx` 简历导入；
9. PDF、`.docx`、Markdown、纯文本作品集导入；
10. 导入内容识别、归类、去重提示和用户确认；
11. AI 资料检查与可选完善建议；
12. 多目标岗位保存；
13. 手动粘贴 JD；
14. 岗位职责、必备技能和加分项提取；
15. 岗位要求与个人经历证据匹配；
16. 匹配报告；
17. 一页、两页选择；
18. 简洁单栏和技术型双栏模板；
19. 栏目勾选、排序和双栏位置调整；
20. 当前岗位经历的必须使用、不使用和自动决定；
21. STAR、CAR、成果优先、技术岗位、应届生、简洁表达、JD 关键词、量化和 ATS 友好策略；
22. 多步骤简历生成工作流；
23. 事实一致性、岗位覆盖和页面长度检查；
24. 简历工作台和实时预览；
25. 文字修改指定段落；
26. 在线语音转文字、文字确认后修改指定段落；
27. 修改前后对比、理由、证据、接受和拒绝；
28. 当前草稿自动保存；
29. 用户主动创建历史版本；
30. 版本查看、对比、恢复、删除和重新导出；
31. Word 和 PDF 导出；
32. 本地备份；
33. 恢复前自动备份和整包恢复；
34. 清除全部本地数据；
35. 空白、加载、网络失败、API 失败、解析失败和导出失败状态；
36. 安装新版本时保留原有本地数据。

## 25. 第一版暂时不做

- 原 AI 日程 App 的聊天、日历、提醒和“今日计划”；
- 账号注册、登录和多账号；
- 云端部署和云数据库；
- 多位求职者档案切换；
- 英文简历；
- 模拟面试；
- 正式面试实时提词；
- 投递跟踪；
- 自动投递；
- 自动与 HR 沟通；
- 自动读取 BOSS、智联等招聘网站；
- 批量爬取岗位；
- 扫描件和图片 OCR；
- 自动扫描完整代码目录或 Git 仓库；
- 任意 Word、PDF 原始模板复刻；
- 用户自定义规则库；
- 带占位符的自定义 Word 模板；
- DeepSeek 等多模型供应商切换；
- 完全离线大模型；
- 本地语音模型；
- macOS 和 Linux；
- 自动更新；
- 正式 Windows 代码签名；
- Microsoft Store 上架；
- 备份文件加密；
- 多份备份数据智能合并；
- 复杂逐字版本合并；
- 自动生成或允许 AI 主动编造虚假经历。

## 26. 可实际检查的验收标准

以下标准用于第一版功能验收。除明确说明外，验收环境为 Windows 10 和 Windows 11 各至少一台测试机。

### 26.1 安装与启动

- 安装包可以完成安装并创建桌面快捷方式；
- 未预装项目开发环境的测试机可以启动程序；
- 首次启动进入隐私与初始化流程；
- 关闭后重新启动，已保存资料仍然存在；
- 覆盖安装新构建版本后，原有本地资料仍然存在。

### 26.2 API 配置

- 用户可以输入、隐藏和显示 API Key；
- 有效 Key 可以通过连接测试；
- 无效 Key 显示明确错误且不覆盖原有有效配置；
- 跳过配置后可以填写资料，但 AI 功能明确不可用；
- 日志、SQLite、备份和导出文件中不得出现完整 API Key。

### 26.3 个人资料

- 用户可以完成所有已定义栏目和自定义字段的新增、编辑、复制、删除；
- 用户可以添加、更换和显示个人照片；
- 所有个人资料栏目和照片均为选填，留空时可以正常保存和继续；
- 用户不需要为不存在的经历填写“无”，空栏目不会出现在生成结果中；
- 自动保存后强制关闭程序，再次启动时最近修改仍存在；
- 完全没有工作经历的用户不会被要求生成虚假工作经历；
- “当前求职方向”不出现在个人信息固定字段中。

### 26.4 文件导入

- 至少使用 5 份不同排版的可选择文字 PDF 和 5 份 `.docx` 简历测试；
- 系统能够提取正文并生成可确认的归类结果；
- 不确定内容不会未经确认直接写入资料；
- 检测到疑似重复经历时出现合并、保留、替换、忽略选项；
- 扫描 PDF 显示不支持 OCR 的明确提示；
- 损坏或加密文件不会导致程序崩溃。

### 26.5 资料检查

- 对只包含少量信息的测试经历可以给出可选完善建议；
- 点击建议可以定位到对应经历或字段；
- 用户可以忽略职责、技能、行动、成果或量化数据的补充建议，并继续保存、分析岗位和生成简历；
- 系统只分析用户实际提供的内容，不得因可选字段为空而生成或猜测对应事实；
- 只有经历记录完全没有可用内容时，生成前才需要提示用户修改或移除该空记录；
- AI 检查不会自动篡改个人资料。

### 26.6 多岗位隔离

- 同一用户可以建立至少 10 个目标岗位；
- 每个岗位保存独立 JD、配置、草稿和版本；
- 岗位 A 的“不要用于本次简历”设置不影响岗位 B；
- 删除岗位 A 不得删除个人资料或岗位 B；
- 删除岗位前必须二次确认。

### 26.7 岗位分析和匹配

- 对包含职责、必备要求和加分项的中文 JD，可以输出结构化结果；
- 每条匹配结论可以追溯到一个或多个个人资料条目；
- 没有证据的要求显示“缺少证据”，不得伪造匹配经历；
- 用户补充资料后可以重新匹配并获得更新结果；
- 网络或 API 失败时，JD 和岗位草稿不丢失。

### 26.8 简历配置

- 用户可以选择一页或两页；
- 用户可以选择单栏或双栏模板；
- 用户可以勾选栏目并拖动排序；
- 双栏模板可以把栏目移动到左栏或右栏；
- 选中空栏目时必须提示补充或取消；
- 必须使用的经历出现在生成结果中；
- 不用于当前岗位的经历不得出现在生成结果中；
- 内置写作策略可以被选择并实际影响生成表达。

### 26.9 生成与真实性

- 生成过程显示可理解的步骤和状态；
- 取消生成后已填写内容不丢失；
- 测试资料未提供数字时，生成结果不得自行增加具体成绩数字；
- 测试资料未提供公司或职位时，生成结果不得自行增加公司或职位；
- 事实检查发现无依据内容时必须阻止其直接进入正式版本；
- 用户可以删除风险内容、改为不含数字的表达或补充证据；
- 一页版和两页版的导出页数与用户选择一致，超出时在导出前阻止并提示。

### 26.10 AI 段落修改

- 用户选中一个段落后，AI 默认只修改该段落；
- 修改前后内容、理由和证据同时显示；
- 未点击接受时原文不变；
- 点击拒绝后原文保持不变；
- 点击接受后只替换目标内容；
- 新增事实可以选择只用于当前简历或同时保存到个人资料库；
- 用户要求编造经历时，系统不给出可直接用于正式简历的虚假经历。

### 26.11 语音修改

- 用户可以主动开始和停止录音；
- 录音可以转成中文文字；
- 转写文字未经用户确认不得直接发送修改；
- 转写失败时可以重新录音或切换文字输入；
- 转写成功并确认后，临时录音默认删除；
- 麦克风权限被拒绝时，程序不崩溃且文字功能可用。

### 26.12 草稿和历史版本

- 普通修改只更新草稿，不自动增加历史版本数量；
- 点击“保存版本”后新增一个历史版本；
- 历史版本可以查看、改名、改备注、对比、恢复、导出和删除；
- 历史版本只读，不能直接修改；
- 当前草稿有未保存修改时，恢复版本必须提供保存、放弃或取消选择；
- 恢复历史版本不删除该历史版本。

### 26.13 Word 和 PDF 导出

- 单栏和双栏模板均可导出 Word 和 PDF；
- 导出文件可以正常打开；
- 姓名、电话、邮箱和照片在本地正确填回；
- 导出前能够发现占位符、空栏目和未确认风险内容；
- 导出失败时当前草稿和历史版本不丢失；
- 导出成功后可以打开文件或所在文件夹。

### 26.14 备份、恢复和清除

- 备份包含个人资料、照片、源文件、岗位、草稿、版本和配置；
- 备份中不包含 API Key；
- 导出备份前显示敏感信息提示；
- 恢复前自动创建当前数据备份；
- 恢复后数据与选定备份一致；
- 使用损坏备份恢复失败时，恢复前数据仍可用；
- 清除全部数据前必须输入指定确认文字；
- 用户可以决定清除时是否同时删除 API Key。

### 26.15 稳定性与错误处理

- API 超时、网络中断、无额度、模型不可用均显示可理解错误；
- 错误发生后用户资料、JD 和草稿不丢失；
- 文件解析失败、导出目录不可写、磁盘空间不足时程序不崩溃；
- AI 正在运行、导出、备份或恢复时关闭软件，必须给出明确提示；
- 日志可以帮助定位错误，但不得记录完整敏感资料和完整 API Key。

## 27. 后续阶段

本 PRD 确认后，后续依次进行：

1. 视觉设计规范；
2. 技术架构设计；
3. 本地数据结构设计；
4. AI 工作流与提示词设计；
5. Word / PDF 模板设计；
6. 开发任务拆分；
7. 第一版实现；
8. 按本 PRD 验收标准测试；
9. Windows 安装包制作与真机测试。
