# 影子简历助手 0.1.4

- 首次配置和设置页新增自定义 Base URL；
- 文本生成、连接测试和语音转写共用配置的网关地址；
- 留空时继续使用 OpenAI 官方 API；
- 支持中转网关自定义模型名和非 `sk-` 前缀密钥；
- 远程网关强制 HTTPS，本机地址允许 HTTP；
- 拒绝包含账号密码、查询参数、片段或具体 `/responses` 路径的错误地址；
- 新增 URL 规范化、持久化、Provider 传递、语音传递和界面流程测试。
