# 影子简历助手 V1 验收报告

日期：2026-08-22  
本机环境：Windows 11 x64，Python 3.11，Node.js 24，Electron 39  
候选版本：0.1.4

## 结论

开发计划阶段 0—9 的实现、自动化测试、本机打包及安装冒烟已经完成。当前候选包可供本机试用。正式标记为 V1 发布版之前，仍必须完成第二台干净 Windows 电脑、Windows 10、真实 OpenAI Key 和真实麦克风的外部人工验收；这些项目没有被跳过，也没有被虚报为通过。

## PRD 第 26 章追踪

| 用例 | 期望与步骤摘要 | 实际结果与证据 | 状态 |
|---|---|---|---|
| ACC-26.1 安装与启动 | 安装、启动本地服务、实际渲染 React 页面、普通用户数据路径启动、卸载 | `tests/windows/verify_package.ps1` 检查安装、资源、健康进程、`#root` 挂载和可见文字；后端、隔离渲染及普通路径渲染退出码均为 0 | 本机通过；第二机待测 |
| ACC-26.2 API 配置 | Key 安全保存，换 Key 前连接测试，错误可理解 | Credential、设置 API、真实 Provider 的 mock 集成测试通过；Key 不进入 DB/备份 | 自动化通过；真实 Key 待测 |
| ACC-26.3 个人资料 | 增删改查、选填栏目、照片 | API 与界面测试覆盖资料、任意经历和照片导入/删除 | 通过 |
| ACC-26.4 文件导入 | PDF/DOCX/TXT/MD 导入并确认 | 解析器、导入候选和确认流集成测试通过；扫描 PDF 限制已记录 | 通过 |
| ACC-26.5 资料检查 | 不擅自覆盖，候选由用户确认 | 候选/决策服务测试通过 | 通过 |
| ACC-26.6 多岗位隔离 | 每个岗位保留独立选择、报告和草稿 | 仓储及 API 隔离测试通过 | 通过 |
| ACC-26.7 岗位分析与匹配 | 结构化 JD，证据状态可追溯 | 严格 JSON Schema、原文引用校验和匹配测试通过 | 自动化通过；真实模型待测 |
| ACC-26.8 简历配置 | 栏目勾选、排序、单条排除 | 配置领域与页面组件测试通过 | 通过 |
| ACC-26.9 生成与真实性 | 生成完整草稿，禁止无来源事实 | 编排器、脱敏和事实检查硬门测试通过 | 自动化通过；真实模型待测 |
| ACC-26.10 AI 段落修改 | 只修改目标段，先建议再接受 | 段落 ID/哈希、建议接受和拒绝测试通过 | 自动化通过；真实模型待测 |
| ACC-26.11 语音修改 | 录音、转写、再作为修改指令 | 浏览器媒体与 Provider mock 测试通过 | 真机麦克风待测 |
| ACC-26.12 草稿与历史 | 小改不存版本；主动保存才建快照 | 快照、命名/备注、比较、恢复、历史导出/删除测试通过 | 通过 |
| ACC-26.13 Word/PDF | 可打开、内容一致、单双页排版 | 四组 QA 样本通过 OOXML/PDF 审计；PDF 已渲染目检 | PDF 通过；Word 真机目检待测 |
| ACC-26.14 备份恢复清除 | 完整性、回滚、防 Zip Slip、清除前备份 | 哈希、非法路径、恢复事务和清除集成测试通过 | 通过 |
| ACC-26.15 稳定性 | 可恢复错误、后端重启、性能达标 | 后端崩溃重启、错误分类、100/50/200 数据量性能和约 1.2 秒启动测试通过 | 本机通过；长时间探索待测 |

## 已运行的质量门

- Python Ruff 静态检查；
- Python 单元与集成测试；
- React/TypeScript 类型检查与 Vitest；
- Electron 进程生命周期和打包后端测试；
- JSON Schema 和 OpenAPI 客户端测试；
- DOCX/PDF 生成及版面审计；
- Windows 安装、启动、资源、后端和卸载冒烟。

## 外部验收清单

1. 在一台未装开发环境的 Windows 11 电脑安装、运行、卸载；
2. 在 Windows 10 x64 重复完整主流程；
3. 用有效 OpenAI Key 实测连接、岗位解析、生成、修改及各类错误；
4. 用真实麦克风在安静/有噪声环境各完成一次中文转写和修改；
5. 用 Microsoft Word 打开四组 QA DOCX，检查字体、照片、装饰、单双页和第二页占用；
6. 检查 SmartScreen 提示与 SHA-256；
7. 完成 30 分钟以上连续创建、修改、保存、导出、备份和恢复探索测试。
