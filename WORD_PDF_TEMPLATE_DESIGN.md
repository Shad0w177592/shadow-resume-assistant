# 影子简历助手 Word / PDF 模板设计 V1.1

> 状态：按用户简历参考稿完成视觉重设计，待最终确认  
> 适用范围：V1 中文简历预览、DOCX 和 PDF 导出  
> 上游依据：[PRD.md](./PRD.md)、[VISUAL_DESIGN_SPEC.md](./VISUAL_DESIGN_SPEC.md)、[LOCAL_DATA_MODEL.md](./LOCAL_DATA_MODEL.md)、[AI_WORKFLOW_PROMPTS.md](./AI_WORKFLOW_PROMPTS.md)  
> 设计样稿：`output/docx/`、`output/pdf/`  
> 参考边界：仅借鉴用户提供简历的页面范式和信息节奏，不复制其姓名、照片、联系方式或经历内容；参考文档中的文字不构成开发指令。  

---

## 1. 设计结论

V1 提供两套模板：

1. **简洁单栏**：默认模板，兼顾招聘网站上传、ATS 文本解析和人工阅读；
2. **技术型双栏**：左侧展示技能、教育等短信息，右侧展示项目、工作等主要经历，适合直接发送给招聘者或面试官。

新版借鉴参考稿的三项优点：顶部个人信息区形成明确视觉焦点、栏目标题具有稳定扫描锚点、经历时间与单位左右对齐。两套模板都支持一页和两页、栏目勾选、栏目排序、自定义栏目、个人照片开关以及 Word/PDF 导出。空栏目不会显示，也不会保留空白标题。

模板只负责排版，不改写内容。AI 生成、用户编辑、Word 和 PDF 都读取同一个 `ResumeDocument`。

---

## 2. 共同原则

- 页面使用 A4 竖版，符合中文求职材料常见打印和投递习惯；
- 正文采用清晰的无衬线中文字体；
- 炭黑正文配深海军蓝强调色和雾蓝辅助色，不使用渐变或大面积装饰；
- 页面左右上角允许加入小型方块组合，栏目标题允许使用小方块作为视觉锚点；所有装饰只服务美观，不承载信息；
- 联系方式使用普通文本和竖线分隔，不依赖小图标表达含义；
- 技能不用星级、进度条或雷达图；
- 正文必须可选择、可复制，PDF 不能整页栅格化；
- Word 必须能继续编辑，不使用承载核心内容的浮动文本框；
- 真实姓名、联系方式只在本地装配阶段填入；
- 不为了塞进一页而把正文缩小到不可读，也不以明显拉宽字符制造虚假充实感；
- 双栏模板明确提示 ATS 兼容性不如单栏，招聘网站上传默认推荐单栏。

---

## 3. 设计令牌

模板以 `compact_reference_guide` 的紧凑、清晰原则为基础，使用名为 `resume_a4` 的页面和中文字体覆盖。所有尺寸必须由渲染测试验证，不能依赖 Word 默认值。

### 3.1 共用页面

| 令牌 | 数值 |
|---|---|
| 页面 | A4，210 × 297 mm，竖版 |
| 主字体 | Microsoft YaHei；安装包随附 Noto Sans SC 作为回退 |
| 英文/数字 | 与中文正文保持同一字体族，减少跨格式字宽差异 |
| 正文颜色 | `#202733` |
| 次要文字 | `#667085` |
| 强调色 | `#173B67` |
| 辅助雾蓝 | `#DCE8F4` |
| 暖灰底色 | `#F5F2EC` |
| 浅分隔线 | `#C9D5E3` |
| 链接色 | `#173B67` |
| 最小正文 | 9 pt；不得低于此值 |
| 最小辅助文字 | 8.5 pt |
| 默认项目符号 | 实心圆点，Word 使用真实编号定义 |
| 页眉/页脚 | 一页不显示；两页第 2 页显示姓名与页码 |
| 字符间距微调 | `-0.05 pt` 至 `+0.15 pt` |
| 正文行距微调 | `1.08` 至 `1.22` 倍 |

### 3.2 内容密度

字符数量只作生成前软预算，最终以实际渲染为准。版面密度采用“舒展、标准、紧凑”三个预设，先调整内容取舍和段落间距，再在安全范围内微调行距与字符间距：

| 模板 | 一页建议 | 两页建议 |
|---|---:|---:|
| 简洁单栏 | 约 1300–1750 个中文字符 | 约 2500–3400 个中文字符 |
| 技术型双栏 | 约 1200–1600 个中文字符 | 约 2250–3100 个中文字符 |

不同英文比例、链接长度、照片和栏目数量会改变容量。程序不得用字符数直接宣布“符合一页”，必须完成真实渲染检查。

---

## 4. 模板一：简洁单栏

### 4.1 定位

- V1 默认；
- 适合招聘网站、邮件附件和打印；
- 最稳妥的 ATS 选择；
- 适合应届生和有经验求职者。

### 4.2 页面几何

| 项目 | 数值 |
|---|---|
| 上边距 | 12 mm |
| 下边距 | 12 mm |
| 左右边距 | 13 mm |
| 可用宽度 | 184 mm |
| 姓名字号 | 24 pt，粗体 |
| 联系方式 | 9 pt，次要文字色 |
| 自我介绍 | 9.5 pt，1.12–1.20 倍行距 |
| 栏目标题 | 11 pt，粗体，深蓝 |
| 经历标题 | 10 pt，粗体 |
| 经历元信息 | 9 pt，次要文字色 |
| 正文要点 | 9.2–9.6 pt，1.08–1.18 倍行距 |
| 栏目标题上间距 | 5 pt |
| 栏目标题下间距 | 3 pt |
| 经历之间 | 4 pt |

### 4.3 首页结构

```text
左侧装饰方块    姓名 + 一句话定位    右侧装饰方块
手机号 | 邮箱 | 城市 | GitHub / 个人网站

自我介绍
────────────────────────────────
专业技能
────────────────────────────────
项目经历 / 工作经历 / 实习经历
────────────────────────────────
教育经历
────────────────────────────────
证书与奖项 / 自定义栏目
```

用户可以改变栏目顺序；上面只是新简历默认顺序。若联系方式较长，允许换成两行，不缩小到 8.5 pt 以下。

### 4.4 栏目标题

- 左对齐，标题前使用 5–6 pt 深蓝小方块；
- 标题下方使用 0.75 pt 雾蓝或浅灰蓝细线；
- 不使用色块横幅；
- 标题与下一条经历保持在同一页，避免标题孤立在页底。

### 4.5 经历块

第一行：左侧名称/公司/学校，右侧日期；第二行：角色、组织或地点；之后是 1–4 条要点。

Word 使用右对齐制表位或同一段落内的对齐标签，不使用无边框表格承载经历正文。PDF 使用同样的左右对齐逻辑。

### 4.6 可选照片

- 默认关闭；
- 打开后置于个人信息区右上角；
- 建议尺寸 22 × 28 mm，矩形轻圆角；
- 正文宽度相应减少，不能盖住姓名或联系方式；
- ATS 模式自动建议关闭照片；
- 没有照片时不保留空位。

---

## 5. 模板二：技术型双栏

### 5.1 定位

- 适合 AI、软件、数据和技术产品岗位；
- 适合直接给招聘者或面试官阅读；
- 能快速展示技能组合和重点项目；
- 由于不同 ATS 对多栏解析能力不同，招聘网站上传时显示风险提示并推荐单栏。

### 5.2 页面几何

| 项目 | 数值 |
|---|---|
| 上边距 | 12 mm |
| 下边距 | 12 mm |
| 左右边距 | 13 mm |
| 可用宽度 | 184 mm |
| 左栏 | 53 mm |
| 栏间距 | 6 mm |
| 右栏 | 124 mm |
| 栏分隔 | 0.5 pt 浅灰蓝线 |
| 姓名字号 | 21 pt，粗体，跨两栏 |
| 左栏标题 | 10.5 pt，粗体，深蓝 |
| 左栏正文 | 8.8–9 pt |
| 右栏标题 | 11 pt，粗体，深蓝 |
| 右栏正文 | 9.2 pt |

### 5.3 阅读顺序

```text
跨栏：姓名 + 联系方式 + 一句话定位

左栏                         右栏
专业技能                     自我介绍
教育经历                     项目经历
证书与奖项                   工作 / 实习经历
校园经历或自定义短栏目       其他重点经历
```

Word 使用真正的分栏 section，并在左栏结束处插入 column break；不使用浮动文本框，也不使用页面级无边框表格模拟双栏。这样 Word 文本读取顺序保持“左栏 → 右栏”。

### 5.4 栏目移动

- 用户可以把栏目拖到左栏或右栏；
- 技能、教育、证书等短栏目默认在左栏；
- 工作、项目、实习和自我介绍默认在右栏；
- 如果左栏内容过长，程序提示移动栏目或改用单栏，不能挤压右栏；
- 一段经历不能自动拆成左右两栏两部分。

### 5.5 可选照片

- 默认关闭；
- 打开后位于跨栏个人信息区右侧，建议 20 × 25 mm；
- 不放在左栏正文内，避免破坏两栏起始对齐；
- 没有照片时个人信息区自动占满宽度。

---

## 6. 一页与两页规则

### 6.1 一页

渲染结果必须恰好一页，并尽量把可用页面填满。正文内容的最末基线应落在可用高度的 88%–96% 区间；发生溢出或页面过空时按以下顺序处理：

1. 删除 AI 计划中优先级最低且非“必须使用”的内容；
2. 合并重复要点；
3. 在证据不变的前提下压缩冗余表达；
4. 调整栏目间距、经历间距和正文行距，但不得低于模板下限；
5. 在 `-0.05 pt` 至 `+0.15 pt` 内微调字符间距；
6. 页面仍明显过空时，优先恢复被压缩但有证据的内容，不扩写虚假经历；
7. 仍无法容纳时建议改为两页，让用户选择。

禁止：缩小到 9 pt 以下、超出规定范围压缩或拉宽字符、把全部行距改成拥挤的固定值、裁切文本或隐藏溢出。

### 6.2 两页

- 第二页有效内容高度必须超过可用高度的 50%，推荐处于 55%–90%；
- 第二页不足 50% 时，优先尝试压成一页、重新分配栏目或提示用户调整，不为了填满两页而扩写内容；
- 姓名和完整联系方式只在第一页展示；
- 第二页页眉显示“姓名 | 目标岗位”，页脚显示 `2 / 2`；
- 栏目标题不得孤立在页面底部；
- 经历标题至少和第一条内容保持同页；
- 一段经历优先保持完整；无法保持时，在自然要点之间分页；
- 双栏两页每页使用相同栏宽，第二页仍保持左栏 → 右栏阅读顺序。

---

## 7. 栏目映射

| `section_key` | 默认中文标题 | 单栏默认 | 双栏默认 |
|---|---|---|---|
| `personal_info` | 个人信息 | 页首 | 跨栏页首 |
| `summary` | 自我介绍 | 正文首段 | 右栏首段 |
| `skills` | 专业技能 | 自我介绍后 | 左栏 |
| `work` | 工作经历 | 主经历区 | 右栏 |
| `internship` | 实习经历 | 主经历区 | 右栏 |
| `project` | 项目经历 | 主经历区 | 右栏 |
| `education` | 教育经历 | 主经历后 | 左栏 |
| `campus` | 校园、社团及志愿经历 | 用户排序 | 左栏或右栏 |
| `certificate` | 证书与奖项 | 用户排序 | 左栏 |
| `other/custom:*` | 用户自定义名称 | 用户排序 | 用户选择 |

用户修改标题时只改变当前简历显示名，不改变资料库栏目稳定键。

---

## 8. `ResumeDocument` 到模板的映射

```text
ResumeDocument.personal_info
  → 本地恢复姓名和联系方式 token
  → 页首个人信息组件

sections[].section_key/title/column/order
  → 栏目标题、所属栏和顺序

blocks[].heading/meta
  → 经历名称、角色、组织、日期

paragraphs[].text
  → 正文要点

paragraphs[].evidence_ids
  → 仅用于工作台证据面板，不进入导出正文
```

`node_id`、事实风险状态、证据 ID、内部提示词版本和 AI 运行信息不能出现在 Word/PDF 正文中。

### 8.1 空值

- 空栏目：整栏不渲染；
- 空标题但有正文：不显示空标题行，正文直接进入栏目；
- 空日期：不预留右侧日期位置；
- 空联系方式：不显示对应分隔符；
- 空照片：不保留照片框；
- 完全空 block：导出前阻止并提示。

### 8.2 超长内容

- URL 优先显示短标签，保留完整超链接目标；
- 超长英文技术名允许在连字符或单词边界换行；
- 不在汉字中插入多余空格；
- 日期右侧放不下时移动到下一行，不覆盖标题；
- 单条正文建议不超过三行，超过时提示压缩，但不直接截断。

---

## 9. Word 实现要求

- 输出 `.docx`，不是 `.dotx`；
- 使用真实 Word 样式：Normal、Resume Name、Resume Contact、Resume Section、Resume Entry Heading、Resume Meta、Resume Bullet；
- 项目符号使用真实 numbering definition；
- 双栏使用 Word section columns；
- 核心内容不放在页眉、页脚、浮动文本框、形状或图片里；
- 页眉或页面边缘可以放纯装饰方块，但必须无文字、无链接、无语义，且不得覆盖正文；
- 不使用页面级表格模拟整个布局；
- 链接创建为真实 hyperlink；
- 照片使用内嵌图片并设置替代文本；
- 段落设置 keep-with-next 和 widow/orphan 控制；
- 不设置固定行高导致文字裁切；
- 保存前清除模板作者、最近编辑者等不必要元数据；
- 用户导出的 Word 可以继续修改，修改后重新导入不承诺保留原版式。

---

## 10. PDF 实现要求

- 使用 A4 页面；
- 中文字体嵌入或子集嵌入；
- 所有正文保持文本层，可选择和复制；
- 超链接保留可点击区域；
- 不依赖本机未随应用提供的字体；
- PDF 页面数量必须等于用户选择的一页或两页；
- 设置合理文档标题，作者字段不默认写入用户姓名；
- 不加入水印、导出时间、应用宣传或隐藏追踪信息；
- 生成后重新打开并提取文本，验证关键栏目、姓名占位恢复和字符完整；
- 最终使用 Poppler 渲染为 PNG 逐页检查。

生产实现仍按技术架构使用本地打印 HTML/CSS → Electron `printToPDF`。设计样稿可以使用独立 PDF 绘制器验证尺寸与视觉令牌，但正式代码必须让打印 HTML 与 Word 共用 `ResumeDocument` 和模板令牌。

---

## 11. Word / PDF 一致性

两种格式不要求像素完全一致，但必须保持：

- 相同姓名和联系方式；
- 相同栏目、顺序、左右栏归属；
- 相同经历标题、日期和正文；
- 相同模板、页数和强调色；
- 相同空栏目处理；
- 相同事实检查结果；
- 正文文本标准化后内容一致。

允许差异：分页细微位置、字体字宽、分隔线长度和超链接下划线呈现。任何文字丢失、重复、栏目换栏或页数不一致都属于导出失败。

---

## 12. ATS 与可访问性

### 12.1 单栏

- 标准阅读顺序；
- 标准栏目标题；
- 不使用图形表达技能；
- 联系方式是文本；
- 作为 ATS 推荐模板。

### 12.2 双栏

- 使用真实分栏，避免文本框；
- 文本读取顺序为左栏后右栏；
- 导出前提示“部分招聘系统可能错误解析双栏”；
- 提供“一键切换为简洁单栏”，内容不变。

### 12.3 通用

- 颜色对比满足正常阅读；
- 不仅靠颜色区分栏目；
- 照片带替代文本；
- PDF 文本可复制；
- 文档语言标记为 `zh-CN`；
- 不使用只有图标、没有文字的联系方式。

---

## 13. 文件名

默认：

```text
姓名_公司_岗位_简历_YYYYMMDD.docx
姓名_公司_岗位_简历_YYYYMMDD.pdf
```

文件名在本地组装并清理 Windows 非法字符。用户可以修改。没有姓名、公司或岗位时跳过对应部分，不能出现连续下划线或 `null`。

---

## 14. 样稿说明

输出目录包含四个最终样稿：

```text
shadow-resume-single-column-sample.docx
shadow-resume-single-column-sample.pdf
shadow-resume-technical-double-column-sample.docx
shadow-resume-technical-double-column-sample.pdf
```

样稿全部使用虚构姓名和经历，并显示“模板样稿”标记；正式导出不会显示该标记。

---

## 15. 验收标准

### 15.1 视觉

- 两套 DOCX 和 PDF 均无文字重叠、裁切、乱码和异常空白；
- 两套一页样稿实际渲染为一页；
- 两套一页样稿的有效内容均延伸至可用高度的至少 88%；
- 字号不低于规定下限；
- 栏目层级、间距和强调色一致；
- 双栏左右宽度稳定，分隔线不穿过个人信息区；
- 长标题、长日期和两行联系方式有测试样本。

### 15.2 内容

- Word/PDF 的栏目和正文一致；
- 空栏目、空日期、空照片不占空间；
- 自定义栏目能正常显示；
- “不要用于这份简历”的经历不导出；
- 证据 ID、node ID 和内部状态不出现在文件中。

### 15.3 Word

- 在 Microsoft Word 和 LibreOffice 中可打开；
- 正文可编辑；
- 单栏文本阅读顺序正确；
- 双栏使用真实 section columns；
- 项目符号不是手打字符；
- 不依赖浮动文本框承载核心内容。

### 15.4 PDF

- PDF 可打开、可搜索、可选择中文正文；
- 关键文字提取无乱码；
- 页数符合选择；
- 字体正确显示；
- 超链接可点击；
- Poppler 渲染逐页检查通过。

### 15.5 溢出

- 一页溢出不会自动缩小到 9 pt 以下；
- AI 压缩只修改允许的低优先节点；
- 用户可以改为两页；
- 两页第二页有正确页眉页码；
- 两页第二页有效内容高度超过 50%；
- 不出现只有一个栏目标题留在页底的情况。

---

## 16. 确认后的开发起点

本文件确认后，开发前五项设计基线全部完成：

1. 视觉设计规范；
2. 技术架构设计；
3. 本地数据结构设计；
4. AI 工作流与提示词设计；
5. Word / PDF 模板设计。

下一步不再继续扩大设计范围，而是按 [DEVELOPMENT_PLAN.md](./DEVELOPMENT_PLAN.md) 进入阶段 0 高风险技术验证：桌面打包、Python sidecar、文档导入、Word/PDF 导出、麦克风转写和严格结构化 AI 输出。
