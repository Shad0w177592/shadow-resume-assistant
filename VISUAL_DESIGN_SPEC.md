# 影子简历助手视觉设计规范 V1

> 状态：待用户确认  
> 适用版本：V1.0 Windows 桌面端  
> 概念参考：[visual-concept-v1.png](./docs/design/visual-concept-v1.png)  
> 依据：[PRD.md](./PRD.md)

## 1. 视觉定位

影子简历助手采用“浅色、专业、简洁、可信赖”的生产力工具风格。

关键词：

- 专业，不像娱乐或社交应用；
- 安静，不使用强烈渐变、霓虹或科幻 AI 装饰；
- 清晰，重要任务入口和状态一眼可见；
- 克制，少用阴影、胶囊和装饰性图形；
- 可信，AI 建议、风险和本地隐私状态表达明确；
- 高效，适合长时间填写资料和编辑简历。

## 2. 整体方向

- 主内容区使用浅灰白背景；
- 内容卡片使用白色；
- 左侧主导航使用深海军蓝；
- 主操作使用沉稳蓝紫色；
- 成功、警告、危险状态使用低饱和语义色；
- 不使用深色主画布；
- 第一版不做完整深色模式；
- 不显示用户头像或“个人用户”，因为第一版无账号和登录；
- 首页不显示复杂统计图表；
- AI 不使用机器人头像或拟人形象，以“AI 建议”“证据”“风险”功能状态体现。

## 3. 色彩规范

### 3.1 基础色

| Token | 建议颜色 | 用途 |
|---|---|---|
| `color-bg-app` | `#F6F7FA` | 应用主背景 |
| `color-bg-sidebar` | `#0B2855` | 左侧导航 |
| `color-bg-surface` | `#FFFFFF` | 卡片、弹窗、输入区 |
| `color-bg-subtle` | `#F0F3F8` | 次级区块、禁用背景 |
| `color-border` | `#DDE3EC` | 卡片和输入框边界 |
| `color-border-strong` | `#B9C4D3` | 强调边界和分隔 |

### 3.2 品牌与交互色

| Token | 建议颜色 | 用途 |
|---|---|---|
| `color-primary` | `#315BD6` | 主按钮、链接、选中状态 |
| `color-primary-hover` | `#274DB8` | 主按钮悬停 |
| `color-primary-active` | `#203F95` | 主按钮按下 |
| `color-primary-soft` | `#E9EEFF` | 选中导航、轻提示 |
| `color-focus` | `#7F95F4` | 键盘焦点环 |

### 3.3 文字色

| Token | 建议颜色 | 用途 |
|---|---|---|
| `color-text-primary` | `#172033` | 标题和正文主文字 |
| `color-text-secondary` | `#536075` | 次级说明 |
| `color-text-muted` | `#7A8699` | 时间、占位和弱信息 |
| `color-text-inverse` | `#FFFFFF` | 深色背景文字 |
| `color-text-link` | `#315BD6` | 文本链接 |

### 3.4 语义色

| Token | 建议颜色 | 用途 |
|---|---|---|
| `color-success` | `#198754` | API 正常、已保存、检查通过 |
| `color-success-soft` | `#E9F6EF` | 成功提示背景 |
| `color-warning` | `#A66A16` | 待确认、可选完善建议 |
| `color-warning-soft` | `#FFF5DF` | 警告提示背景 |
| `color-danger` | `#C63D4D` | 事实风险、删除和失败 |
| `color-danger-soft` | `#FDECEF` | 危险提示背景 |
| `color-info` | `#356AC3` | 普通信息和进度 |
| `color-info-soft` | `#EAF2FF` | 信息提示背景 |

语义色不能作为唯一信息表达方式，必须同时包含图标、文字或状态标签。

## 4. 字体规范

### 4.1 应用界面

建议字体栈：

```text
"Microsoft YaHei UI", "Noto Sans SC", "Segoe UI", sans-serif
```

### 4.2 字号层级

| 层级 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| 页面标题 | 24/34 | 600 | 首页、个人资料等页面标题 |
| 区块标题 | 18/28 | 600 | 当前草稿、最近岗位 |
| 卡片标题 | 16/24 | 600 | 岗位名、版本名 |
| 正文 | 14/22 | 400 | 表单、说明和列表 |
| 辅助文字 | 13/20 | 400 | 时间、状态说明 |
| 小标签 | 12/18 | 500 | 状态标签、计数 |

正文最小字号原则上不低于 12px；关键操作和长文本不低于 14px。

## 5. 间距与网格

采用 4px 基础单位和 8px 主节奏：

```text
4 / 8 / 12 / 16 / 24 / 32 / 40 / 48
```

推荐值：

- 应用基础设计尺寸：1440×900；
- 最小窗口：1280×800；
- 主导航宽度：224px；
- 页面水平内边距：32px；
- 页面垂直内边距：24px；
- 大区块间距：24px；
- 卡片内部间距：24px；
- 表单字段纵向间距：16px；
- 标签与输入框间距：8px；
- 图标与文字间距：8px。

窗口小于最小尺寸时允许出现滚动，不通过无限压缩破坏工作台布局。

## 6. 圆角、边框和阴影

- 输入框和按钮圆角：6px；
- 卡片圆角：8px；
- 弹窗圆角：10px；
- 状态标签圆角：4px，不使用过度胶囊化；
- 普通边框：1px `color-border`；
- 悬浮卡片可使用极轻阴影；
- 主页面卡片优先使用边框，不依赖明显阴影区分层级；
- 禁止玻璃拟态、强模糊和发光效果。

建议轻阴影：

```text
0 2px 8px rgba(16, 35, 64, 0.06)
```

## 7. 图标规范

- 使用统一的线性图标风格；
- 常规尺寸 20px，紧凑区域 16px；
- 导航图标 20～22px；
- 默认线宽保持一致；
- 图标必须配合文字或可访问名称；
- 不混用填充、彩色拟物和线性图标；
- 产品第一版不需要独立吉祥物或机器人图标。

## 8. 导航规范

左侧固定导航顺序：

1. 首页；
2. 个人资料；
3. 目标岗位；
4. 设置。

导航规则：

- 深蓝背景，白色或浅灰文字；
- 当前项使用浅蓝半透明背景和白色文字；
- 悬停只改变背景，不做位移动画；
- 底部可显示版本号；
- 不显示用户头像、账号菜单或登录入口；
- 工作台、匹配报告和历史版本通过页面标题与返回按钮体现层级。

## 9. 按钮规范

### 9.1 类型

- Primary：生成、继续编辑、保存版本等主要操作；
- Secondary：导入、检查、重新尝试；
- Ghost：返回、取消、次级工具；
- Danger：清除数据、删除岗位和版本；
- Link：查看全部、打开报告。

### 9.2 尺寸

- 默认高度：40px；
- 紧凑工具栏：32px；
- 大型主入口：44px；
- 水平内边距：16～20px；
- 图标按钮点击区域不小于 36×36px。

### 9.3 状态

每个按钮必须设计：默认、悬停、按下、焦点、禁用和加载状态。

危险操作不能只用红色文字，必须有明确动词和二次确认。

## 10. 表单规范

- 默认输入框高度 40px；
- 标签位于输入框上方；
- 选填字段可标注“选填”，不要求用户填写“无”；
- 不使用大量星号暗示必填；
- 错误信息紧邻对应字段；
- 帮助文字只说明实际影响，不制造压力；
- 长经历使用可自动增高文本框；
- 自动保存状态放在页面顶部，不反复弹 Toast；
- AI 可选建议使用轻警告样式，可以忽略；
- 空选填栏目不显示负面红色状态。

## 11. 卡片和列表

- 首页使用 2 列主卡片布局；
- 最近岗位和版本使用清晰列表或卡片，不做数据图表；
- 卡片标题、状态、时间和主操作建立稳定对齐；
- 同一卡片只保留一个主要按钮；
- 更多操作进入 `···` 菜单；
- 有未保存修改使用警告状态，不使用危险红色；
- 事实风险才使用危险状态。

## 12. 首页视觉规则

首页信息优先级：

```text
继续当前草稿
→ 新建目标岗位 / 导入已有简历
→ 资料状态
→ 最近岗位
→ 最近版本
```

资料状态卡可以显示当前已有资料的可用状态和建议数量，但不得因为用户没有填写选填栏目、没有照片、没有技能或成果而显示为“不完整”。如果使用百分比，计算只能基于用户已经创建的记录和未确认问题；更推荐显示“资料已保存 / 有 N 条可选建议”。

## 13. 简历工作台视觉规则

- 左栏：固定工具区，宽度约 240～280px；
- 中间：浅灰画布，白色 A4 页面居中；
- 右栏：AI 修改助手，宽度约 320～360px；
- A4 页面使用轻边框和微弱阴影；
- 当前选中段落使用蓝色边框和淡蓝背景；
- AI 建议不直接覆盖页面，先在右栏展示差异；
- 证据引用使用可点击的轻量标签；
- 事实风险使用红色边框、风险图标和明确文字；
- 一页/两页溢出使用页面底部标记，不通过缩小字体隐藏。

## 14. 状态反馈

### 14.1 成功

使用短暂 Toast 或页面内状态，例如“已自动保存”“版本已保存”。

### 14.2 加载

长任务显示实际步骤：分析 JD、匹配证据、生成、检查，而不是只显示旋转图标。

### 14.3 空状态

使用一句原因说明和一个主要操作，不使用装饰性大插画。

### 14.4 错误

错误必须说明：发生了什么、数据是否保留、用户下一步能做什么。

### 14.5 风险

“可选完善建议”和“事实风险”必须明显区分：

- 可选建议：黄色/信息色，可忽略；
- 事实风险：红色，保存正式版本和导出前必须处理。

## 15. 动效规范

- 常规过渡：120～180ms；
- 抽屉和弹窗：180～240ms；
- 使用淡入、颜色和高度变化；
- 禁止弹跳、旋转装饰和大范围位移；
- 用户启用减少动态效果时关闭非必要动画；
- AI 长任务进度应真实反映阶段，不能用虚假百分比循环。

## 16. 无障碍与 Windows 适配

- 正文文字对比度至少达到 WCAG AA；
- 键盘焦点始终可见；
- 表单标签与控件建立程序关联；
- 图标按钮提供可访问名称和 Tooltip；
- 错误不能只通过颜色表达；
- 支持 100%、125%、150% Windows 缩放；
- 主要页面支持键盘 Tab 顺序；
- 常用操作支持 Enter、Escape 和标准复制粘贴；
- 不拦截系统级文本选择和辅助技术。

## 17. 概念图修正规则

概念图只用于确认风格，不作为像素级最终稿。正式设计必须修正：

- 移除右上角“个人用户”和头像；
- 资料状态不得把空的选填栏目视为缺失；
- 所有示例公司和岗位使用虚构数据；
- 首页不出现个人敏感信息；
- 具体布局以 PRD 已确认的信息结构为准；
- 组件尺寸、颜色和文字以本规范为准。

## 18. 视觉验收清单

- [ ] 所有页面使用“影子简历助手”名称；
- [ ] 主导航只有首页、个人资料、目标岗位、设置；
- [ ] 无登录、头像和账号入口；
- [ ] 所有选填字段不会显示必填错误；
- [ ] 可选建议与事实风险视觉区分明确；
- [ ] 主按钮在每个区域只有一个；
- [ ] 颜色、字体、间距和圆角使用统一 Token；
- [ ] 1280×800 下功能可用；
- [ ] 125% 和 150% 缩放下不遮挡关键操作；
- [ ] 键盘焦点清晰；
- [ ] 空、加载、失败和禁用状态完整；
- [ ] 工作台 A4 页面、左右栏和风险标记清晰；
- [ ] 不使用渐变、玻璃拟态、霓虹和科幻装饰；
- [ ] 概念图中的账号元素已移除。

